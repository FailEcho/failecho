# Runtime dependencies (see pyproject.toml for the canonical list).
fastapi>=0.111
pydantic>=2.7
sqlalchemy>=2.0
aiosqlite>=0.20
uvicorn[standard]>=0.30
mcp>=2.0
opentelemetry-proto>=1.20

# Dev / test
pytest>=8.0
pytest-xdist>=3.5    # `pytest -n 2`: the suite in about 60% of the time
httpx>=0.27
