# Runtime dependencies (see pyproject.toml for the canonical list).
fastapi>=0.111
pydantic>=2.7
sqlalchemy>=2.0
aiosqlite>=0.20
uvicorn[standard]>=0.30
mcp>=2.0

# Dev / test
pytest>=8.0
httpx>=0.27
