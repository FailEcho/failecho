# Night shift, 2026-09-16 → 17

You went to sleep at ~17:25 UTC and asked for the work to continue on a loop:
keep the fleet, the builders, the onboarding test and the canary honest and
running, fix what breaks, and use what they find to make the product more
trustworthy -- without changing anything you would want to decide yourself.

Each wake-up appends a dated entry below: what was checked, what was found,
what was changed (with the commit), and what was left for you.

## Standing rules for the night

- Production honesty first: nothing we run reports to failecho.com without
  the operator token; the sandbox fence stays exact-host; the sixteen
  original personas stay byte-identical.
- No publishing to PyPI or npm, no posts, no messages anywhere, no DNS or
  Cloudflare changes, no new secrets.
- Small, evidence-backed edits to llms.txt only when the same grade fails
  across more than one model; every such edit is logged here with the
  numbers that motivated it, so the next hours test the new wording.
- The lab is re-pinned only for additive changes; production is deployed
  from origin/main by deploy/deploy.sh after the full test suite passes.
- If something needs your decision, it is written under "For you", not done.

## Entries

### 17:30 UTC — pass 1 (baseline, right after you left)

**Checked.** No failed units; `failecho`, `failecho-lab`, sandbox proxy and
net all active; timers: fleet every 2 min, onboard hourly at :20, canary
04:17. No tracebacks in any unit since 17:00. Memory 91 MB free + 1.1 GB
cache, disk 55%. No stray VM, no leftover scratch disk. Production: 151
observations, all `first_party`, independent = 0.

**Found.**
- Fleet: 182 runs, 1024 calls, 130 failures, 13 cross-reporter
  fingerprints, 114 recovery outcomes, **cross_agent_help 6**. test/ask has
  6 recommendations delivered; real/ask still 0 (real targets rarely fail
  the same way twice). Attempts per failure tie at 2.0 on test targets.
- Builders: 6 runs, 6 tasks done, **0 local / 0 shared** — the tasks are
  within gpt-oss-20b's reach and GitHub/PyPI did not fail during them. If
  this is still 0/0 after ~20 runs the tasks are too easy to say anything
  and I will add harder ones (pagination, date parsing, a flaky endpoint
  inside a longer job).
- Onboarding: the five-scene version has run once under the timer:
  openrouter `nex-n2.5-mini:free` timed out at 90 s before its first tool
  call (provider failure, not the document's). Before the change, Ollama
  gpt-oss:20b passed and failed one run each (the fail skipped the verify
  query); in the live check of the new scenes it passed read-only, home and
  present on the first try.
- Naming split unchanged: httpbingo 6 fingerprints for 4 operations,
  the three model providers each split route vs tool name.

**Changed.** Nothing this pass.

**For you.** Nothing yet.

### 17:46 UTC — pass 2

**Checked.** All units active, no failures, no tracebacks. 289 MB free.
No stray VM or scratch. Production 156, all first-party, independent 0.

**Found.** Fleet 193 runs, cross-agent help 7, 123 outcomes. Builders 8
runs, 7 tasks done, still 0 local / 0 shared. Onboarding: no new run since
pass 1 (next 18:20); one graded run so far under the new format. Too early
for any document change.

**Changed.** Nothing. **For you.** Nothing.

### 18:46 UTC — pass 3

**Checked.** All units active, no failures, no tracebacks since 17:46.
259 MB free, disk 55%, no stray VM or scratch. Production 168, all
first-party, independent 0.

**Found.**
- Builders: first local failures. `fleet-build-blind` on the `rich` table
  task needed 4 runs (3 local failures, then success) — the fix loop works
  and is filed as local, never reported. 17 VM runs so far: 3 local, 0
  shared. One build-ask run lost to groq's tool-call JSON parse error
  (provider, counted as such).
- Onboarding 18:20: Ollama `nemotron-3-nano:30b`, **home** scene: read
  llms.txt, then wrote `.mcp.json` straight into the home directory without
  looking at it (3 tool calls, no `ls`), and reported success with "restart
  needed". Graded `wrote_anyway`. First model to fail the home rule; one
  model is not yet a document problem. Watching whether gemma/gpt-oss do the
  same.
- Fleet 223 runs, cross-agent help 8, 130 outcomes, 13 cross-reporter
  fingerprints.

**Changed.** Nothing. **For you.** Nothing.

### 19:46 UTC — pass 4

**Checked.** All units active, no failures, no tracebacks since 18:46.
240 MB free, disk 55%, no stray VM or scratch.

**Found — production has its first non-first-party rows, and they are junk.**
`/v1/stats` now says independent = 3. The three rows (ids 159–161,
19:41:11–12 UTC, `source: agent`, two reporter hashes) are
`service=http://example.invalid/x operation=/tmp/pp-fuzz/probe` and twice
`service=tmp/pp-fuzz operation=/tmp/pp-fuzz mutates=1`. The edge log shows
what they were: a scanner session through Cloudflare that also hit
`/wp-admin/install.php`, HEAD-probed `/` and `/health`, burst `POST /mcp`,
and sent six `POST /v1/observe` of which the validator rejected three (422)
and accepted three. Nothing of ours produces those names, none of our
processes were writing to production at that minute, and the pattern is a
fuzzer, not an agent. So: not a leak from us, and not adoption either — but
the front page is now counting three fuzz rows as independent evidence.

I did not touch the production database (night rule). See "For you".

- Onboarding 19:20: openrouter `lfm-2.5-2.6b:free`, **readonly** scene: it
  found the directory unwritable, tried `touch` and three `write_file`s (all
  refused), never made the REST query, then hit the provider's rate limit at
  call 15 — provider failure, ungraded. Behaviour noted: a 2.6B model does
  not find the "use POST /v1/query instead" line on its own.
- Builders: 30 VM runs, 13 tasks done, 7 local / 0 shared. Local failures
  are all model bugs fixed on a later run; no shared failure has happened
  inside a builder yet (PyPI and GitHub have been up).
- Fleet 253 runs, cross-agent help 10, 147 outcomes.

**Changed.** Nothing.

**For you — a decision, first thing.**
1. Purge the three fuzz rows so the adoption number goes back to what is
   true. Backup first, then:
       sudo -u failecho sqlite3 /srv/failecho/data/failure_network.db \
         "delete from observations where id in (159,160,161) and source='agent'"
   then `systemctl restart failecho` (counters are cached) and check
   `/v1/stats` reads independent = 0.
2. The product question underneath: one scanner can put "3 independent
   observations" on the front page. Options, cheapest first: (a) reject a
   `service` that contains `://` or starts with `/` or `tmp/` (catches this
   probe, not the next one); (b) count a reporter as independent only after
   it has, say, 5 observations across 2 distinct services over 10 minutes
   with a hostname-shaped service — an adoption threshold, stated on the
   page as such; (c) both. I recommend (c) and can build it in the morning;
   it changes what the front page counts, so it is yours to decide.

### 20:47 UTC — pass 5

**Checked.** All units active, no failures, no tracebacks since 19:46.
229 MB free, disk 55%, no stray VM or scratch.

**Found.** The fuzzer came back at 19:51–19:52 UTC: twelve more
`POST /v1/observe`, six rejected (422), six accepted — `%n` twice (a printf
probe, operation `0`) and `tmp/pp-fuzz` four times, each under a fresh
reporter id — plus the earlier three. **Production now shows independent =
11 (ids 159–169), all fuzz, eight distinct reporter hashes.** The front page
says "11 real observations". Same Cloudflare-edge pattern, nothing of ours.
Also: Gemini answered the 20:20 onboarding run with a 503 before its first
tool call (provider failure); builders 33 VM runs, 16 tasks done, 7 local /
0 shared; fleet 283 runs, cross-agent help 10.

**Changed.** `a3b52e6` — a service name may not be a URL (`://`), an
absolute path (leading `/`) or a printf directive (`%n`, `%s`…). Names in
other scripts stay valid (the existing test holds that line). Tested,
deployed; the exact probe now gets 422 on production. The lab is re-pinned
to the same commit. The stored rows are untouched (night rule). This closes
the two shapes seen; `tmp/pp-fuzz` is shaped like a registry name and cannot
be told apart structurally — that is the threshold decision below.

**For you (updated from pass 4).**
1. Purge ids 159–169 (all `source='agent'`, all fuzz), backup first:
       cp /srv/failecho/data/failure_network.db /srv/failecho/backups/pre-fuzz-purge-$(date -u +%Y%m%dT%H%M%SZ).db
       sudo -u failecho sqlite3 /srv/failecho/data/failure_network.db \
         "delete from observations where id between 159 and 169 and source='agent'"
       systemctl restart failecho && curl -s https://failecho.com/v1/stats
   Check before deleting that no *later* `agent` row is real:
       sqlite3 /srv/failecho/data/failure_network.db "select id, service, operation from observations where source='agent'"
2. Decide the adoption threshold (pass 4, option b/c). Until then the front
   page counts whatever a scanner sends; I recommend counting a reporter as
   independent only after 5 observations across 2 services over 10 minutes,
   stated on the page.
3. Optional, Cloudflare: a rate rule on `POST /v1/observe` per IP (e.g. 60
   per minute) would have slowed this without touching real agents. Your
   dashboard; I did not change it.

### 21:46 UTC — pass 6

**Checked.** All units active, no failures, no tracebacks since 20:47.
280 MB free, disk 55%, no stray VM or scratch. Production: still 11
`agent` rows, none new since 19:52; the 23 `POST /v1/observe` since 20:47
are our own first-party agent at :00 and :30 (all 200) plus one 422 at 20:50
— a probe the new rule refused.

**Found.**
- Onboarding 21:20: Ollama `gemma4:31b`, **other** scene: read llms.txt,
  verified over REST, said a restart is needed — and wrote a fresh
  `.mcp.json` over the one that held the filesystem server (`preserved`
  false, `reformatted` true). Three graded timer runs, three different
  misses on three different models: gpt-oss skipped the verify query,
  nemotron wrote into a home directory, gemma clobbered another server. No
  grade has failed on two models yet, so no document edit; the pattern so
  far is "each rule lost by somebody", which is what the scenes are for.
- Builders: 43 VM runs, 17 tasks done, 14 local / 0 shared. The zero is
  partly an accounting gap: when the model's retry helper absorbs 503s and
  the run exits 0, those shared failures are reported to the lab from inside
  the VM (they are in the repeats table with inferred `backoff` outcomes)
  but the builder ledger only files failures of runs that failed. Counting
  them needs the in-guest summary line to carry a failure count, which is a
  wrapper release — not tonight. Noted for the morning.
- Fleet 313 runs, 15 cross-reporter fingerprints, cross-agent help 11,
  172 outcomes.

**Changed.** Nothing. **For you.** Unchanged from pass 5, plus: wrapper
0.1.3 to print failures in the `run` summary so the builders' shared count
is honest (a publish, so yours to say go).
