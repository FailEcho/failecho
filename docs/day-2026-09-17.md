# Day watch, 2026-09-17

Same rules as `docs/night-2026-09-16.md`. Hourly passes while the fleet
collects; each pass appends checked / found / changed (commit) / for you.

Triggers being watched:

- **5** onboarding home scene: a second model (other than nemotron) writing
  into the home directory after the 23:46 llms.txt edit -> restructure the
  default section as a numbered checklist.
- **6** builders: shared failures still under ~10% of failures after each
  of the eight harder tasks has run twice -> add tasks on rate-limited
  services.
- **7** explorer-c: `conditional_request` outcomes under a live GitHub 403
  -> keep or drop the action.
- **8** naming table: splits on `not_found` / `validation_error` classes
  after the new tools -> a per-service alias map.
- production honesty: independent must stay 0 unless a real stranger shows
  up; any `agent` row gets looked at.

## Entries

### 07:48 UTC — pass 1

**Checked.** Units active; timers fine; 115 MB free + 1 GB cache; disk 56%;
no stray VM. Production 338, all first-party, independent 0, sparse 0.

**Found — and fixed.** Eight fleet runs since 06:20 finished their work,
reported to the lab and saved state, then died writing the scoreboard:
`KeyError: 'failures'`. My 06:00 coverage loop reused the cohort's variable
name (`c`), so the next failure counted on a builder run with coverage data
indexed a coverage entry instead. No data lost (state saves before the
report); `/fleet` was stale for ~90 minutes. Fixed in `b6145c5` with the
ledger test now carrying coverage and a failure together; lab re-pinned;
scoreboard writing again at 07:50.

**Data.** 615 runs, cross-agent help **76**, 470 outcomes. Test cohort
ask 1.92 vs blind 2.00, 10 skips on advice. Real-services table (fair-order
runs only, since 06:30): GitHub ask 19 failures / 1.63 attempts / 5 skipped
/ 37 s, blind 2 failures / 1.00 — too few blind rows yet to read; the swap
means ask ran second this cycle. GitHub recovery evidence on the lab last
6 h: backoff 3/49, wait_until_reset 0/1 — 3-second backoff basically does
not fix GitHub's limit, which the network will soon say as `skip`. Builders:
88 VM runs, 32 done, 22 local / **21 shared** — the rate-limit-aware task
absorbed 18 real 429s and 0.1.3 counted them (traffic 36 / observed 31).
Explorers: 10 explorations, 3 provider recoveries. Onboarding unchanged at
9 graded / 3 passed: the 07:20 home-scene run (gpt-oss-20b) died on groq's
rate limit before acting — trigger 5 not fired.

**Changed.** `b6145c5`. **For you.** Nothing.

### 08:48 UTC — pass 2

**Checked.** All active, no tracebacks since the fix, 116 MB free + 1 GB
cache, no stray VM. Production 350, all first-party, independent 0.

**Found.** Onboarding 08:20, Ollama `gpt-oss:20b`, **other** scene: added
the entry beside the filesystem server, kept it intact — then *offered* the
verify curl to the user instead of running it ("if you want to test a
query, you can run…"). Graded `verified` false. That is this model's second
miss on the same line (clean scene, 18:xx yesterday). Reading: the
document's "Then verify… over REST" reads to a small model as advice to the
human, not an instruction to the agent. Trigger 5 is about the home scene
and a second model; this is a different line and still one model — noted,
not acted on. Scenes: other 2/4, present 1/1, clean 0/1, home 0/3, readonly
0/1; 10 graded / 3 passed.

Fleet 643 runs, cross-agent help 78, 20 cross-reporter fingerprints.
Test cohort ask 1.91 vs blind 2.00, 12 skips. Real-services (fair order):
GitHub ask 19 f / 1.63 / 5 skipped vs blind 4 f / 1.50 — blind still thin.
GitHub outcomes last 3 h: backoff 1/16, wait_until_reset 0/1. Explorers 12
explorations, 4 provider recoveries of 13. Builders 92 VM runs, 33 done,
23 local / 21 shared.

**Changed.** Nothing. **For you.** Nothing.

### 09:49 UTC — pass 3

**Checked.** All active, no tracebacks, 98 MB free + 1.1 GB cache, no stray
VM. Production 362, all first-party, independent 0.

**Found.** The network now says `skip` for GitHub's 403 (backoff 1/21 in the
last 3 h), and the asking twin acts on it: real-services table, fair order,
GitHub ask 39 failures / **1.56 attempts / 15 skipped** vs blind 8 failures /
1.75 / 0. The failure *counts* differ because the twins share one address
and one hourly budget — whoever runs second meets more 403s, and a blind
twin's retries burn budget too — so counts are not a fair comparison; per
failure behaviour (attempts, seconds, skipped) is. Caption on the table says
so now. Cross-agent help **93** (was 65 at 06:16; most of the rise is skip
advice delivered). Test cohort ask 1.89 vs blind 2.00, 16 skips. Explorers
13 explorations, 4 of 15 provider failures recovered. Onboarding 09:20:
openrouter nex rate-limited before acting; 10 graded / 3 passed unchanged.
Naming: GitHub still splits `GET /repos` vs `github_repo` on 404 (pooling
covers the 403, not the 404) — trigger 8 watching, not yet acting.

**Changed.** Caption only (`deploy`, re-pin). **For you.** Nothing.

### 10:48 UTC — pass 4

**Checked.** All active, no tracebacks, 129 MB free + 1 GB cache, no stray
VM. Production 372, all first-party, independent 0.

**Found.**
- First cost rows (8 runs a side, real cohort, since 10:30): ask **273
  tokens/run** vs blind **432**; 1.6 s/run vs 2.4; wait 0.38 s vs 0.75;
  asking cost 0.015 s/run; completed 62.5% both. Test cohort, one run each:
  ask 15.5 s vs blind 28.3 s — two skips (always_broken, slow_read) save
  ~13 s a run. Early; direction as expected.
- GitHub outcomes last 3 h: backoff 2/27, wait_until_reset 0/1,
  **conditional_request 0/1** — the first data point for trigger 7, a miss;
  need more before keep-or-drop.
- Onboarding 10:20: nemotron, home scene, did `ls -la` this time (the edit
  moved it to look) and wrote `.mcp.json` into the home directory anyway.
  Home 0/4, all nemotron ×3 + gpt-oss-20b ×1 pre-edit; trigger 5 (second
  model post-edit) still not fired. 11 graded / 3 passed.
- Fleet 704 runs, cross-agent help 106, 526 outcomes, 21 cross-reporter
  fingerprints. Explorers 15 explorations, 4/19 provider recoveries.
  Builders 95 VM runs, 33 done, 24 local / 21 shared.

**Changed.** Nothing. **For you.** Nothing.

### 11:48 UTC — pass 5

**Checked.** All active, no tracebacks, 167 MB free + 1 GB cache, no stray
VM. Production 384, all first-party, independent 0.

**Found.** Costs (real cohort, 18 ask / 20 blind runs): completed 61% vs
50%; tokens/run 356 vs 459; seconds/run 3.5 vs 2.4 (ask waits on advice
0.67 s vs 0.60 s and asks cost 0.019 s; the rest is provider recoveries the
asker attempts and blind does not). Test cohort (2 runs each): ask 23.4 s vs
blind 32.9 s. GitHub, fair order: ask 45 f / 1.58 / 15 skipped vs blind
14 f / 1.71 / 3 recovered. GitHub outcomes 6 h: backoff 4/46,
wait_until_reset 0/2, conditional_request 0/1. Onboarding 11:20: lfm 2.6B
rate-limited before acting (readonly); 11 graded / 3 passed. Fleet 733 runs,
cross-agent help 108. Builders 97 VM runs, 33 done, 25 local / 21 shared.

**Changed.** Nothing. **For you.** Nothing.

### 12:48 UTC — pass 6

**Checked.** All active, no tracebacks, 133 MB free + 1 GB cache, no stray
VM. Production 396, all first-party, independent 0.

**Found — and fixed.** Every builder run since the harder tasks went in
(10:30) ended "(provider failed: rate_limit)": a longer task's context —
program plus traceback plus stdout — crosses groq's 8,000 tokens/minute on
the second or third call, and the builder loop had no wait for it. Both
twins now wait the minute out, up to three times (counted in
`wait_seconds`), and tool results fed back are trimmed to 1,500 chars a
side (`8068bed`). Costs (real, 30/30 runs): completed 60% vs 53%, tokens
361 vs 400, seconds 3.3 vs 2.1. Test cohort: ask 24.1 s/run vs blind 33.7.
GitHub 6 h: backoff 3/37, wait_until_reset 0/2, conditional_request 0/1.
Onboarding 12:20: gemini rate-limited before acting; 11 graded / 3 passed.
Fleet 763 runs, cross-agent help 114.

**Changed.** `8068bed`. **For you.** Nothing.

### 13:48 UTC — pass 7

**Checked.** All active, no tracebacks, 218 MB free, no stray VM.
Production 406, all first-party, independent 0.

**Found.**
- 13:20 onboarding: Ollama `gemma4:31b`, **other** scene — `ls -la`, added
  the entry beside the filesystem server intact, verified, said a restart is
  needed: PASS. Same model clobbered the other server yesterday 21:20 (that
  was before the "look first" edit). 12 graded / 4 passed; other 3/5.
- The builders' minute-waits worked once (ask, 31.8 s, done) and then the
  real wall arrived: groq's **tokens-per-day** quota for gpt-oss-20b
  (200,000) ran out at ~13:00 UTC. Every groq persona — decor-a pair, mcp
  pair, builders, explore-a — was dead until midnight. Gemini's daily quota
  is gone too. Free tiers are the experiment's ceiling; today it is where
  the onboarding test's 23 KB document (~30k tokens a run on groq) had been
  spending most of the budget.
- Costs (real, 39/39): completed 61.5% vs 56.4%, tokens 391 vs 382,
  seconds 2.9 vs 2.1. Test cohort 28.2 s vs 33.1 s. GitHub 6 h: backoff
  3/32, wait_until_reset 0/1, conditional_request 0/1.

**Changed.** `cebce8b` — the provider's error body rides on the exception;
a builder meeting a per-day quota switches to the alternate model (both
twins alike: an account fact, not a network decision) instead of waiting;
the onboarding test's groq slot moves from gpt-oss-20b to
llama-3.3-70b-versatile, which has its own budget. Tests, deploy, re-pin.

**For you.** Nothing.

### 14:48 UTC — pass 8

**Checked.** All active now; 260 MB free; no stray VM. Production 414, all
first-party, independent 0.

**Found — and fixed, both mine.** (a) The 13:50 change that switches a
builder's model on a daily quota referenced a `state` the builder loop did
not have; from ~13:55 every builder run died with NameError before saving,
so the round-robin re-ran the same builder every two minutes for ~35 min and
starved the other personas. Fixed `2857d85` with a test that drives a
"tokens per day" 429 through the builder loop. (b) Meanwhile the token work:
the MCP tool list every client pastes into the model each turn was ~2,700
tokens; descriptions and parameter text rewritten and schema titles
stripped, now ~1,690 (`83a1965`, `5440a2d`), instructions 305 → 200, and a
check answer 540 → 180 (`fa3588c`). A size budget test guards the list.

Costs (real, 48/48): completed 65% vs 56%, tokens per completed **671 vs
627** (+7%, was +12% before the asker's payload was trimmed), seconds 2.9
vs 2.1. Cross-agent help 123. GitHub 6 h: backoff 3/30, wait_until_reset
0/1, conditional_request 0/1. Onboarding 14:20: openrouter ling rate-limited
before acting; 12 graded / 4 passed. Test cohort 25.1 s vs 31.3 s.

**Changed.** `2857d85`, `83a1965`, `5440a2d`. **For you.** Nothing.

### 15:48 UTC — pass 9

**Checked.** All active, no tracebacks since 15:05, 165 MB free, no stray
VM. Production 428, all first-party, independent 0.

**Found — and fixed.** The 15:20 onboarding run (groq slot) died at once:
`llama-3.3-70b-versatile` is not on this account's model list any more
(404). I had named it from memory at 13:50. The account lists
`openai/gpt-oss-120b` and `qwen/qwen3.8-27b`; the onboarding slot and the
builders' quota fallback use those now (`checked`, not remembered).

Costs (real, 60/60): completed 60% vs 55%; tokens per completed **658 vs
687** — the asking side is now cheaper per completed task; model re-calls
after skip 0 since the `retry_pointless` flag. Seconds 2.6 vs 2.0. Test
cohort 24.7 s vs 33.4 s. Builders 109 VM runs, 34 done, 26 local / 31
shared (blind's first shared: 5). GitHub 6 h: backoff 2/16,
wait_until_reset 0/1, conditional_request 0/1. Cross-agent help 126.

**Changed.** Model ids (deploy, re-pin). **For you.** Nothing.

### 16:48 UTC — pass 10

**Checked.** All active, no tracebacks, 94 MB free + 790 MB cache, no stray
VM. Production 440, all first-party, independent 0.

**Found.** Onboarding 16:20: Ollama `gpt-oss:20b`, other scene — `ls -la`,
added beside the filesystem server intact, said restart needed, and again
did not run the verify query. That is this model's third miss on `verified`
(clean 16 Sep 18:xx, other 08:20, other 16:20); gemma and nex both verified
on their passes. Still one model, so under the two-model rule it is a note,
not an edit — but it is the clearest single pattern the test has produced.
13 graded / 4 passed. Costs (real, 70/71): completed 61% vs 58%, tokens per
completed **634 vs 695**, seconds 2.4 vs 2.0, model re-calls after skip 0.
Test cohort 24.8 s vs 35.2 s. Builders 111 VM runs, 36 done, 26 local / 31
shared. GitHub quieter (groq personas out on daily quota): backoff 1/5 in
6 h. Cross-agent help 133.

**Changed.** Nothing. **For you.** If gpt-oss:20b misses `verified` once
more, I will treat it as a document problem despite the rule: the verify
line reads as advice to the human. Say so if you would rather I act now.

### 17:48 UTC — pass 11

**Checked.** All active, no tracebacks, 117 MB free + 750 MB cache, disk
58%, no stray VM. Production 454, all first-party, independent 0.

**Found.** Onboarding 17:20: openrouter nex rate-limited before acting;
13 graded / 4 passed unchanged. Costs (real, 80/79): completed 60% vs 57%,
tokens per completed 628 vs 681, seconds 2.8 vs 1.9, re-calls after skip 0.
Test cohort 25.8 s vs 36.5 s. Builders 119 VM runs, 39 done, 28 local / 31
shared; build cohorts now 30% completed each since the quota fallback.
GitHub fair-order: ask 59 f / 1.64 / 15 skipped / 119 s vs blind 16 f / 1.62
/ 31 s — the asker has met 3.7x more 403s this afternoon; with groq personas
out on quota the cycle rhythm changed and the swap is not balancing budget
exposure the way it did. Per-failure behaviour is the comparison, as the
caption says. Cross-agent help 135.

**Changed.** Nothing. **For you.** Nothing new.

### 18:48 UTC — pass 12

**Checked.** All active, no tracebacks, 136 MB free + 820 MB cache, no
stray VM. Production 464, all first-party, independent 0.

**Found.** Onboarding 18:20: nemotron, home scene, fourth time — no `ls`,
wrote `.mcp.json` into the home directory, said restart. Home 0/5, still
nemotron ×4 + one pre-edit gpt-oss-20b; no second model post-edit yet (the
groq slot has been dead on quota or a bad model id all afternoon; fixed at
15:48, first real groq home run due tomorrow). Costs (real, 91/90): completed
58% both; tokens per completed 697 vs 674 — the gap flipped back toward the
asker by 3%; it moves a few percent an hour and the day's true reading is
"about equal". Seconds 2.9 vs 1.9. Test cohort 26.2 s vs 37.4 s. Builders
121 VM runs, 41 done, 28 local / 31 shared, both cohorts 36% completed.
Cross-agent help 137.

**Changed.** Nothing. **For you.** Nothing new.

### 19:48 UTC — pass 13

**Checked.** All active, no tracebacks, 103 MB free + 820 MB cache, no
stray VM. Production 476, all first-party, independent 0.

**Found.** First coverage misses: one `fleet-build-blind` run (json2csv
task, 19:14) executed four programs that opened 1–2 connections each and
the wrapper observed none — an HTTP client `run` mode does not patch. The
ledger did not say which; it now records the HTTP-related import names of
the generated program (`1410187`), so the next miss names the client.
Costs (real, 102/102): completed 58.8% both; tokens per completed 663 vs
650 (2% apart — equal); seconds 2.7 vs 1.8; test cohort 25.8 s vs 36.2 s.
Builders 133 VM runs, 43 done, 33 local / 31 shared, both cohorts 42%
completed. Onboarding 19:20: lfm rate-limited; 14 graded / 4 passed.
Cross-agent help 140.

**Changed.** `1410187`. **For you.** Nothing new.

### 20:48 UTC — pass 14

**Checked.** All active, no tracebacks, 130 MB free + 840 MB cache, no
stray VM. Production 484, all first-party, independent 0. Fleet passed its
1,000th run.

**Found.** Costs (real, 111/111): completed 59.5% both; tokens per completed
640 vs 648 — equal; seconds 2.6 vs 1.8 (the asker's provider recoveries and
reset waits). Test cohort 24.9 s vs 34.0 s, a steady ~9 s a run saved by
two skips. Builders: 138 VM runs, 47 done, 33 local / 45 shared, both
cohorts 50% completed since the quota fallback; build/ask tokens per done
task 4,538 vs blind 6,522. Onboarding 20:20: gemini quota; 14 graded / 4
passed. Cross-agent help 146.

**Changed.** Nothing. **For you.** Nothing new.

### 21:49 UTC — pass 15

**Checked.** All active, no tracebacks, 268 MB free, no stray VM.
Production 494, all first-party, independent 0.

**Found.** Onboarding 21:20: gemma4:31b, other scene, PASS again (looked,
preserved, verified, said restart) — 15 graded / 5 passed; other 4/7.
Costs (real, 122/122): completed 58% vs 60%, tokens per completed 655 vs
672, seconds 3.5 vs 1.8. Test 24.2 s vs 33.3 s. Builders 140 VM runs, 49
done, 33 local / 45 shared, both 53% completed; build/ask 4,186 tokens per
done vs blind 5,963. Cross-agent help 151. Two stars-table builds opened
~145 connections each for 5 observed calls, imports only `requests`; the
ledger could not say to which host, so coverage entries now carry per-host
deltas (`6809d09`). Also today, on request: the Ask-vs-Blind table on the
lab and a live card on the home page; the 15-column cost table split in
two; every fleet table scrolls inside its own box.

**Changed.** `6809d09` plus the day's page work (`6b70f74`…`a9359e8`).
**For you.** Nothing new.

### 22:48 UTC — pass 16

**Checked.** All active, no tracebacks, 169 MB free, disk 59%, no stray VM.
Production 504, all first-party, independent 0.

**Found.** Quota skipping works: groq, gemini and openrouter are all out for
the day and their personas were skipped 14 times this tick-hour instead of
burning slots; ollama and the model-less personas got the time. Costs
(real, 133/133): completed 60% vs 59%, tokens per completed **614 vs 647**,
seconds 3.5 vs 2.1. Test 22.8 s vs 31.7 s. Builders 141 VM runs, 50 done,
33 local / 63 shared. Onboarding 22:20: openrouter ling rate-limited.
Cross-agent help 157.

The coverage puzzle deepened rather than closed: a rate-limit-aware-client
task (httpbingo only) recorded 181 connections in its window — 111 to
httpbingo, but also 25 to api.github.com, 20 to pypi.org, 12 to
files.pythonhosted.org, 6 to crates.io, 6 to peps.python.org — hosts the
program had no reason to touch. Either the before/after read of the fence's
counters is catching another guest's traffic (only one VM should hold the
lock) or the counters are not what I think. Open; I will test it by hand in
the morning with a trivial program, before trusting the coverage numbers.

**Changed.** Nothing this pass (`42ffc2c` at 22:30). **For you.** Nothing.

### 23:48 UTC — pass 17

**Checked.** All active, no tracebacks, 95 MB free + 670 MB cache, disk
60%, no stray VM. Production 519, all first-party, independent 0.

**Found.**
- **First home-scene pass.** 23:20, groq `openai/gpt-oss-120b` (the slot
  moved off gpt-oss-20b at 15:48): `ls -la`, `ls -la proj-0`, then asked
  which project instead of writing — exactly what the document asks. A
  second model after the "look first" edit, and it passed; trigger 5 (a
  second model *failing*) did not fire. Home 1/6, 16 graded / 6 passed.
- Quota skipping: 49 dead slots reclaimed today. Costs (real, 144/144):
  completed 59% vs 58%, tokens per completed **577 vs 609**, seconds 3.7 vs
  3.0. Test 21.1 s vs 30.0 s. Cross-agent help 182, outcomes 772.
- Coverage counters: the proxy's cumulative per-host totals right now are
  exactly the numbers the 22:14 entry recorded as its "delta" — so the
  before-snapshot is reading as empty and every entry has been the running
  total, not the program's own traffic. The coverage figures on the
  scoreboard are wrong and I will not quote them; fixing it needs the hand
  test in the morning to see why the first read comes back empty.

**Changed.** Nothing. **For you.** Nothing new.

### 00:48 UTC (18 Sep) — pass 18

**Checked.** All active, no failed units, 76 MB free + 854 MB cache, no
stray VM. Production 529, all first-party, independent 0.

**Found.**
- Onboarding 00:20: Ollama `gpt-oss:20b`, other scene, python. Kept the
  filesystem server, wrote the entry, read llms.txt — and did not verify.
  That is this model's **fourth** `verified` miss (16 Sep clean, 17 Sep
  08:20 other, 16:20 other, 18 Sep 00:20 other); `liquid/lfm-2.5-2.6b` missed
  the same grade once on readonly. The rule says two models on one scene;
  in pass 10 I wrote: "If gpt-oss:20b misses `verified` once more, I will
  treat it as a document problem despite the rule." Re-reading the answers:
  each time the model *offered* the curl to the user. The verify bullet
  pointed at a curl further down the page and read as advice to the human.
- Quota: the day rolled over at midnight and every provider is unmarked
  again; groq and gemini reset on their own clocks (rolling / Pacific), so
  the first slots after 00:00 may re-discover a dead provider and re-mark
  it. Expected, one wasted call each.
- Versus (real, 154 vs 156 runs): completed 59.1 vs 58.3 %, tokens per
  completed **581 vs 618**, seconds 3.7 vs 3.3, seconds lost in failures
  0.91 vs 1.43. Test 20.3 vs 29.1 s/run. Build 17 vs 16 runs, 58.8 vs 56.2 %.
- Coverage counters still not to be quoted (pass-17 note stands).

**Changed.** llms.txt verify bullet (`d1965e0`): it now says run it
yourself, do not hand it to the user, carries the request inline, and says
setup is not done until the answer has been read. Tests keep the pinned
phrases; deployed; lab re-pinned to `d1965e0`. Trigger for the next check:
gpt-oss:20b's next graded run on any scene.

**For you.** Nothing new.

### 01:48 UTC (18 Sep) — pass 19

**Checked.** All active, no failed units, 84 MB free + 827 MB cache, disk
60%, no stray VM, no leftover scratch. Production 540, all first-party,
independent 0.

**Found.**
- Onboarding 01:21: openrouter `nex-agi/nex-n2.5-mini`, present scene,
  python. Left the existing config untouched, said it was already
  configured, verified over REST, no reporting, no hook — and was graded
  FAIL on `no_home_edit`, because its `find . -maxdepth 2 -name
  '.claude.json' ...` put the filename in a command. Looking in the project
  is not touching the home file: grader misgrade. The stored row stays a
  fail (history is history); by the corrected grade present is 2/2.
- First graded run after the verify-line edit (`d1965e0`, 00:5x): it ran
  the curl itself. One run, one model; not yet evidence for the edit.
- Stack Exchange: throttled since ~23:26 (`throttle_violation`, "more
  requests available in 11993 seconds"), served as HTTP 400, which the
  wrapper labels `validation_error/400`; 24 failures per cohort, 0
  recovered, ask skipped 11. The verdict is right (skip), the label is
  not; the wrapper classifies from status and exception text and never
  reads bodies, which is by design. Noted, not changed.
- Versus (real, 165 vs 166): completed 58.8 vs 59.0 %, tokens per
  completed 634 vs 611 (ask back above blind this hour — the SE throttle
  hour is heavy on asks that end in skip; watch), test 19.6 vs 28.0 s/run,
  build 17 vs 16 runs 58.8 vs 56.2 %.
- Triggers 5–8: nothing fired (no home-scene graded run this hour, no
  harder task ran twice, no conditional_request outcome, no new split).

**Changed.** Grader: `-name` patterns are dropped before the home-edit
check; test added (`82bce59`). Fleet-side only, no deploy; lab checkout
re-pinned to `82bce59`. **For you.** Nothing new.

### 02:48 UTC (18 Sep) — pass 20

**Checked.** All active, no failed units, 87 MB free + 825 MB cache, disk
60%, no stray VM. Production 566, all first-party, independent 0. Canary
last green 04:13 (17 Sep); next 04:16.

**Found.**
- **Quota marks held groq dead for a day it was alive.** At 00:13 groq said
  "tokens per day: 200000, used 199178" — so its day had *not* reset at
  midnight UTC — and the scheduler marked it dead "until midnight UTC",
  meaning all of 18 Sep. One probe at 02:49: groq answered 200. Gemini's
  free tier resets at midnight Pacific (07:00 UTC); its probe now said
  "limit: 20 ... retry in 21s" (still dead, correctly). Ten groq personas
  and both builders would have been skipped for ~21 hours. No build has run
  since 00:13 for this reason.
- Onboarding 02:24: nemotron, home scene, wrote anyway (expected; excluded
  from trigger 5).
- Versus (real, 176 vs 177): completed 58.0 vs 59.9 %, tokens per
  completed **647 vs 648**, test 18.6 vs 27.2 s/run. Real completion and
  tokens are a tie this hour; the test cohort's 8.6 s/run gain holds.
- Stack Exchange still throttled (32 failures per cohort, ask skipped 16,
  ask 14.8 s vs blind 62.3 s spent on it).
- Triggers 5–8: none fired.

**Changed.** Dead-provider marks now carry a timestamp and expire after
two hours (`FLEET_QUOTA_PROBE_SECONDS`); the next persona on that provider
is the probe, which clears or renews the mark. Same rule in the onboarding
test; old bare-date marks count as expired. Tests + docs/fleet-metrics.md
(`7deb8ed`). Fleet-side only; lab checkout re-pinned to `7deb8ed`. The
first groq persona after the re-pin will be the probe. **For you.**
Nothing new.

### 03:48 UTC (18 Sep) — pass 21

**Checked.** All active, no failed units, 95 MB free + 855 MB cache, disk
60%, no stray VM. Production 579, all first-party, independent 0.

**Found.**
- Quota probe worked as designed: the expired marks let groq's personas
  through at ~03:26; mcp-blind and both builders ran (7–10 s each,
  json2csv task, both completed), then explore-a hit TPD again at "used
  199830 of 200000" and groq was re-marked for 120 min. Gemini's probe
  died on "limit: 20", re-marked. So groq's window is rolling, not a
  Pacific day; the two-hour probe is the right shape for it. 69 slots
  skipped for quota today.
- **Fence refusal filed as a service failure, again, other door.** The
  json2csv task says "test it on the 'slides' array from
  httpbingo.org/json"; both builders called `fetch_doc` on httpbingo.org,
  the doc allowlist raised, and the raise was reported to the lab as
  `httpbingo.org fetch_doc/error` — one row per cohort, both cohorts, so
  the comparison is not skewed, but it is not a failure of httpbingo.
- Onboarding 03:24: `liquid/lfm-2.5-2.6b`, readonly scene: tried to write
  `.mcp.json` fourteen times and ran out of model calls. Model failure,
  graded right.
- Versus (real, 189 vs 189): completed 59.3 vs 60.3 %, tokens per
  completed 654 vs 659, test 18.4 vs 26.9 s/run, build 18 vs 17 runs
  61.1 vs 58.8 %, 5361 vs 5555 tokens per completed.
- Triggers 5–8: none fired.

**Changed.** Builder: an off-list `fetch_doc` host is answered with a tool
message ("documentation only … fetch data from inside run_python") before
anything is wrapped, called or reported; test (`1645c85`). Fleet-side
only; lab checkout re-pinned to `1645c85`. **For you.** Nothing new.

### 04:48 UTC (18 Sep) — pass 22

**Checked.** All active, no failed units, 88 MB free + 805 MB cache, disk
60%, no stray VM. Production 591, all first-party, independent 0. Canary
04:16 green, all paths.

**Found.**
- **Two caps were above the free tiers.** Gemini's 429 body carries
  `GenerateRequestsPerDayPerProjectPerModel-FreeTier, quotaValue: 20` for
  gemini-3.8-flash — twenty requests a day per model, not the 400 our cap
  assumed. OpenRouter's says `free-models-per-day`, `X-RateLimit-Limit: 50`
  without credits; our cap was 120. Neither cap ever mattered in practice
  (the providers enforce theirs first), but the rule is caps never above
  documented tiers, and these were.
- All three remote providers out of quota now (groq rolling TPD, gemini 20
  RPD, openrouter 50 RPD); 96 slots skipped for quota today; only Ollama
  and the no-provider personas ran this hour. Gemini at 20 requests a day
  is not a useful fleet provider: three personas plus onboarding share it.
- Onboarding 04:20: gemini, clean, provider 429 ×3. Not graded.
- Versus (real, 199 vs 199): completed 58.8 vs 60.3 %, tokens per
  completed 653 vs 659, test 17.7 vs 26.1 s/run. Builds unchanged (18/17).
- Triggers 5–8: none fired.

**Changed.** Gemini cap 40 (20 × two models), OpenRouter cap 50; the
onboarding test takes the smaller of its cap and the provider's; test pins
the numbers (`267b231`). Fleet-side; lab checkout re-pinned.
**For you.** Gemini's free tier is 20 requests a day per model. Either it
stays as a token presence in the fleet (a few graded runs a day) or you
swap the slot for a provider with a usable free tier. Your call; nothing
needed today.

### 05:54 UTC (18 Sep) — pass 23

**Checked.** All active, no failed units, 86 MB free + 776 MB cache, disk
60%, no stray VM. Production 616, all first-party, independent 0.

**Found.**
- Onboarding 05:22: Ollama `gemma4:31b`, other scene, node — pass (kept
  the filesystem server, verified over REST, said restart). Gemma is 3/4
  on other; its one miss was `preserved`.
- The provider change (`be0abeb`, 05:45, user-approved "let's try 1,2")
  has had no model-driven run yet: every remote provider carries a mark
  from before it — openrouter until 06:10, gemini 07:18, groq 07:34
  (groq re-marked at 05:34 by a run on the old code). Until those expire
  only Ollama and the no-provider personas run, so the new `provider`
  versus group is not on the scoreboard yet. Expected; first rows after
  06:10.
- Versus (real, 210 vs 210): completed 58.6 vs 60.0 %, tokens per
  completed 643 vs 676, test 17.2 vs 25.7 s/run, build 19 vs 18 runs,
  63.2 vs 61.1 %, 17.1 vs 32.8 s/run. 125 slots skipped for quota today.
- Trigger 7 closed this hour by the user's go-ahead (conditional_request
  dropped: GitHub answers a conditional GET with 403 over the limit). 5, 6,
  8: nothing fired.

**Changed.** Nothing in this pass (the provider change is logged in
docs/fleet-test-plan.md, 05:30 note). **For you.** Nothing new.

### 06:48 UTC (18 Sep) — pass 24

**Checked.** All active, no failed units, 75 MB free + 806 MB cache, disk
60%, no stray VM. Production 637, all first-party, independent 0.

**Found.**
- NVIDIA's five personas all ran in their first cycle: decor-ask-c 10.8 s
  (hand-run probe, 3 model calls, real answer), decor-blind-c 14.0 s,
  both builders ~30 s with 2 model calls each and 0 failures, explore-d
  5.2 s. No 429 from NVIDIA yet.
- The `provider` versus group is on the scoreboard: 6 vs 7 runs, completed
  83.3 vs 85.7 %, one provider failure each side (OpenRouter's 50-a-day,
  both retried once and failed, as the new control says), 0 recovered on
  both. Too small to read; the first explorer evidence for `switch_model`
  is still to come.
- Onboarding 06:21: OpenRouter `ling-3.0-flash-vl`, 429 (the 50-a-day is
  gone by then). Not graded. 147 slots skipped for quota today; groq's mark
  expires 07:34, gemini's 07:18.
- Versus (real, 220 vs 221): completed 58.6 vs 60.6 %, tokens per
  completed 673 vs 672, test 17.1 vs 25.6 s/run, build 20 vs 19 runs 65.0
  vs 63.2 %, 4817 vs 5133 tokens per completed, 17.7 vs 32.6 s/run.
- Triggers 5, 6, 8: nothing fired.

**Changed.** Nothing in this pass (NVIDIA landed at 06:30, `8497e8f`,
user-directed). **For you.** Cerebras key answers "Payment required" —
check that account's billing tab if you want it in.

### 07:48 UTC (18 Sep) — pass 25

**Checked.** All active, no failed units, 77 MB free + 816 MB cache, disk
60%, no stray VM. Production 651, all first-party, independent 0.

**Found.**
- Provider group: 11 vs 11 runs, completed 81.8 vs 81.8 %, four provider
  failures each side, 50 % recovered on both. The recoveries are the
  control's plain retry landing on Gemini 503s ("high demand", transient)
  and one NVIDIA timeout — real, and identical on both sides, as it should
  be before any inherited advice exists. `switch_model` attempts since the
  change: 0; groq's mark expired at 07:34 and explorer-a has not come round
  yet. No dead marks at this moment (first time in a day).
- Onboarding 07:23: NVIDIA `nemotron-3-super-120b`, **home scene**, node.
  Listed the directory, read llms.txt, wrote nothing, quoted the document's
  home-directory rule and stopped — graded FAIL on `asked` because the
  request had no question mark. The stored answer was cut at 300 characters
  so I cannot see the exact wording; the grade was too narrow either way.
  Trigger 5 is not fired on this: it did the right thing (no file) and a
  second real failure is what the trigger asks for. Next home run decides.
- Real (230 vs 230): completed 59.1 vs 61.7 %, tokens per completed 682 vs
  663, test 17.0 vs 25.3 s/run, build 22 vs 21 runs, 68.2 vs 66.7 %, 4470
  vs 4760 tokens per completed, 19.3 vs 32.8 s/run. 158 slots skipped for
  quota today. Mistral and NVIDIA personas ran without a 429.
- Triggers 6, 8: nothing fired.

**Changed.** Grader: "which project / please tell me / let me know" count
as asking when nothing was written; stored answers 300 → 1500 chars; test
(`0cb2bee`). Fleet-side; lab checkout re-pinned. Earlier this hour, by the
user's direction: Mistral added (`10cd702`), OpenCode Zen probed and left
out (free tier only from inside OpenCode; rest paid). **For you.** Which
site issued `XKIRO_API_KEY`? Not touched until you say.

### 08:48 UTC (18 Sep) — pass 26

**Checked.** All active, no failed units, 81 MB free + 790 MB cache, disk
61%, no stray VM. Production 667, all first-party, independent 0.

**Found.**
- **A completed builder run marked groq dead.** 08:39, `fleet-build-blind`:
  TPD on gpt-oss-20b, switched to gpt-oss-120b, finished in 10.8 s — and
  set the dead mark, because the builder marked *before* switching. The
  05:30 rule (mark only when a switch also fails) had not reached the
  builder's own switch path. Groq's askers would have been skipped until
  10:39 for a provider that was serving.
- Onboarding 08:23: Mistral `ministral-14b`, clean scene, python — pass on
  its first graded run (wrote, verified, said INSUFFICIENT_DATA is
  expected). Onboarding is now 10 models deep with a pass from a fifth
  provider.
- Provider group: 18 vs 20 runs, completed 72.2 vs 80.0 %, failures met 8
  vs 6, recovered 37.5 vs 33.3 %. `switch_model` evidence since 05:30:
  Gemini 1/1; groq `retry` 1/2, NVIDIA `retry` 1/1. Explorer-a still has
  not made a switch attempt (groq was marked dead again at 08:39 — see
  above). Too early, as expected.
- Real (240 vs 242): completed 59.6 vs 62.8 %, tokens per completed 719 vs
  701, test 17.3 vs 25.6 s/run, build 23 vs 23 runs, 69.6 vs 69.6 %, 19.2
  vs 30.9 s/run. 160 slots skipped for quota today. xKiro builders ran
  (both completed the numpy task).
- Triggers 5, 6, 8: nothing fired.

**Changed.** Builder keeps the set of models it has tried and marks the
provider dead only when none is left; tests (`5baca9b`). Fleet-side; lab
checkout re-pinned. The wrong 08:39 groq mark cleared from the lab's
state file between runs (openrouter's mark, from a real exhaustion, kept).
Earlier this hour by the user's direction: xKiro added and cap 600
(`3b0cf62`, `d3db2b7`); advice-by-shape and evidence-curve tables on the
scoreboard (`ed4b116`). **For you.** Nothing new.

### 09:48 UTC (18 Sep) — pass 27

**Checked.** All active, no failed units, 75 MB free + 770 MB cache, disk
61%, no stray VM. Production 679, all first-party, independent 0. Only
openrouter marked dead (its 50 a day, real).

**Found.**
- Onboarding 09:23: xKiro `qwen3.6-27b:free`, clean scene, python — pass on
  its first graded run (wrote, verified over REST). Three new providers,
  three first-run passes (nemotron-super on home is the open question).
- Provider group: 25 vs 27 runs, completed 76.0 vs 81.5 %, failures met 9
  vs 7, recovered 33.3 vs 28.6 %. Evidence since 05:30: `switch_model`
  groq 1/1, Gemini 1/1; `retry` groq 1/2, NVIDIA 1/1. Explorer-a made its
  first switch attempt this hour and it worked; four more before the
  server will recommend it. On track for the afternoon, not before.
- Real (249 vs 251): completed 60.6 vs 63.3 %, tokens per completed 757 vs
  738, test 17.7 vs 25.7 s/run, build 26 vs 26 runs, 73.1 vs 73.1 %, 4312
  vs 4295 tokens, 21.1 vs 31.6 s/run. 162 slots skipped for quota today —
  the counter has nearly stopped climbing since the new providers landed
  (147 at 06:48).
- Triggers 5, 6, 8: nothing fired.

**Changed.** Nothing. **For you.** Nothing new.

### 10:48 UTC (18 Sep) — pass 28

**Checked.** All active, no failed units, 75 MB free + 785 MB cache, disk
61%, no stray VM. Production 691, all first-party, independent 0. Only
openrouter marked dead.

**Found.**
- Onboarding 10:24: groq `gpt-oss-120b`, other scene, python — pass
  (kept the filesystem entry, added ours, verified over REST). Other scene
  now 6/10 graded passes.
- Provider group: 33 vs 35 runs, completed 75.8 vs 80.0 %, failures met 11
  vs 10, recovered 27.3 vs 30.0 %. Evidence since 05:30: `switch_model`
  Gemini 2/2, groq 1/1; `retry` groq 1/2, NVIDIA 1/1. Groq did not hit its
  TPD this hour (good for the fleet, slow for the evidence); Gemini's
  explorer made its second successful switch. Askers on Gemini 429 still
  get no recommendation (2 of 5 attempts).
- Real (259 vs 261): completed 61.4 vs 64.0 %, tokens per completed 783 vs
  766, test 17.7 vs 26.2 s/run, build 28 vs 28 runs, 75.0 vs 75.0 %, 4199
  vs 4198 tokens, 23.5 vs 33.4 s/run. Quota skips 162 — unchanged for two
  hours; supply is no longer the limiter.
- Naming table: providers split by path — `POST /v1beta` (auto wrapper,
  named by route) against `chat.completions` (decorator, named by call).
  Rate limits are service-level so the evidence pools anyway; a 400 or
  404 would not. Noted; trigger 8 is not fired by it (it is not a
  not_found/validation_error split between agents naming the same op).
- Triggers 5, 6: nothing fired.

**Changed.** Nothing. **For you.** Nothing new.

### 12:10 UTC (18 Sep) — pass 29

**Checked.** All active, no failed units, 93 MB free + 890 MB cache, disk
61%, no stray VM. Production 706, all first-party, independent 0. Only
openrouter marked dead.

**Found.**
- **gpt-oss:20b verified.** Onboarding 11:23, Ollama `gpt-oss:20b`, clean
  scene, python — pass: wrote the config *and ran the query itself*. This
  is the model that had missed `verified` four times running; first graded
  run since the verify-line rewrite (`d1965e0`, 00:5x) and it did what the
  line now says. One run; the doc change is now supported, not proven.
- Provider evidence since 05:30: `switch_model` groq 3/3, Gemini 3/3 (two
  more each before the server recommends). Provider group 40 vs 42 runs,
  completed 77.5 vs 81.0 %, recovered 25.0 vs 30.0 %; still the no-advice
  tie.
- First OpenCode run inside the fleet unit (12:09, blind twin): pkginfo
  task, 6 steps, 5 tool calls, 60 s, completed, exit 0, ~40k input tokens.
- Real (270 vs 272): completed 62.6 vs 65.1 %, tokens per completed 781 vs
  765, test 18.1 vs 26.2 s/run, build 32 vs 32 runs, 78.1 vs 78.1 %,
  25.2 vs 31.8 s/run. Quota skips 164 (+2 in three hours).
- Triggers 5, 6, 8: nothing fired.

**Changed.** Nothing in this pass. Between passes, by the user's
direction: OpenCode in the sandbox image and two OpenCode personas
(`ce24a9b`, `2bf105b`, fleet unit MemoryMax 1100M); Zen probed and left
out of rotation (free tier answers "Rate limit exceeded", paid path has
no funds). **For you.** Zen: if you used its free quota in the app
today, it may answer tomorrow; I re-probe in the morning.

### 12:55 UTC (18 Sep) — pass 30

**Checked.** All active, no failed units, both lane timers firing (light
12:54, 12:55; VM 12:54), 74 MB free + 744 MB cache, disk 61%, no stray VM.
Production 713, all first-party, independent 0. No provider marked dead.

**Found.**
- **`switch_model` reached the floor on groq: 5/5** since 05:30 (Gemini
  3/3, groq `retry` 2/5). From the next groq 429 the server can recommend
  it and askers inherit; the provider group's tie should start to move
  this afternoon. Provider group now 45 vs 47 runs, completed 75.6 vs
  78.7 %, recovered 33.3 vs 27.3 %.
- Onboarding 12:53: Ollama `nemotron-3-nano`, clean scene — **pass**, its
  first (0/5 before; it had always written into the home directory on the
  home scene; on clean it did the right thing). 12:23: OpenRouter nex,
  429 (50 a day gone).
- Real (278 vs 280): completed 62.6 vs 65.4 %, tokens per completed 778 vs
  761, test 18.1 vs 26.2 s/run, build 34 vs 33 runs, 79.4 vs 78.8 %, 25.0
  vs 31.2 s/run. Quota skips 166 (+2 since 09:48).
- Triggers 5, 6, 8: nothing fired.

**Changed.** Between passes, user-directed: the 48-hour report
(`docs/report-48h.md`, `e779553`, and an artifact page); two lanes with a
locked state file, onboarding every 30 minutes, a second groq explorer
(`1fcfd1c`; units installed, `failecho-fleet-vm.timer` enabled). Nothing
in this pass. **For you.** The report link is in the chat; your intro and
the post are yours.

### 14:31 UTC (18 Sep) — pass 31

**Checked.** All active incl. both lane timers, no failed units, no
tracebacks or OOM. 90 MB free, 418 MB available (an OpenCode guest was up;
lowest reading of the day, watch). Disk 61%. One VM and one scratch file,
both the live VM-lane run's. Production 729, all first-party,
independent 0. Only openrouter dead.

**Found.**
- **Askers are inheriting `switch_model` and it works.** Since 05:30:
  groq `switch_model` 17/17. The journal shows askers meeting groq 429s
  with `rec=switch_model ... recovered=True`. Provider group, 70 vs 72
  runs: **completed 75.7 vs 70.8 %, provider failures recovered 52.2 vs
  31.8 %**, failures met 23 vs 22 (the sample is not skewed). First time
  the completion gap sits on the ask side, and for the reason the design
  says. Still one afternoon; the morning read decides.
- OpenCode: 11 runs, ask twin 5, FailEcho calls 0; ask runs that met a
  failure or did not finish: 2. Trigger 9 (6+) not fired.
- Onboarding 14:22: gemma4 clean — pass. 13:55 Gemini timeout, 13:23
  lfm 429: provider-side, not graded.
- Real (312 vs 315): completed 63.8 vs 65.4 %, test 18.3 vs 26.9 s/run,
  build 51 vs 51 runs 84.3 vs 86.3 %. Quota skips 173.
- Triggers 5, 6, 8, 9: nothing fired.

**Changed.** Nothing in this pass. Between passes, user-directed: OpenCode
wiring checked (connected; the agent chooses not to ask) and
`external_directory` allowed (`64a4475`); coverage counter verified by
hand and quotable again; import reader fixed (`ec1af80`, pushed with a
failing test, fixed in `d1a5e86`). **For you.** Nothing new.

### 15:31 UTC (18 Sep) — pass 32

**Checked.** All active incl. both lanes, no failed units. One traceback
(VM lane). 83 MB free, 443 MB available. Disk 63% (+2% since morning).
One VM and scratch file, the live run's. Production 734, all
first-party, independent 0. No provider dead.

**Found.**
- **Provider group widening for ask:** 85 vs 87 runs, completed **75.3 vs
  67.8 %**, provider failures recovered **55.6 vs 25.9 %**, failures met
  27 vs 27 (same exposure). groq `switch_model` 23/23 since 05:30.
- **Traceback, VM lane:** an OpenCode guest stopped answering and the
  vsock read raised `TimeoutError` through `main()`; the run's record was
  lost. Real breakage.
- **ETag files 21 MB each** (whole npm bodies cached per persona), read on
  every run in a 400 MB lane: the state dir was 180 MB and explains the
  afternoon's low available memory.
- OpenCode: 17 runs, ask twin 8, FailEcho calls 0, ask runs that failed
  or did not finish 2. Trigger 9 not fired.
- Onboarding 15:22 nemotron-super 5xx, 14:53 ling 429: provider-side.
- Real (333 vs 336): 64.0 vs 64.9 %. Build 60 vs 60: 83.3 vs 80.0 %,
  34.0 vs 38.0 s/run. Test 18.9 vs 27.2 s/run.
- Triggers 5, 6, 8, 9: nothing fired.

**Changed.** A guest that does not answer 15 s past its timeout is a
timed-out Result (exit 124), not an exception; the ETag cache keeps 50
entries under 32 KB; existing files shrunk once by the same rule (state
dir 180 MB → 2.3 MB); tests (`18bb6e7`). Lab re-pinned between runs.
**For you.** Nothing new.

### 19:31 UTC (18 Sep) — pass 33

**Checked.** Hourly ticks 16:23–18:23 did not run (the session was busy
or idle-blocked; four queued prompts arrived together), so this pass
covers 15:31–19:31. All active incl. both lanes, no failed units, no
tracebacks or OOM since 15:31. 651 MB available, disk 65%, state dir
2.6 MB. No VM up. Production 776, all first-party, independent 0.

**Found.**
- **NVIDIA hit the fleet's own cap** (400 calls) at ~16:30: OpenCode
  spends 5–14 steps a run. Its four VM-lane personas kept being picked,
  returned "(daily cap reached; run skipped)" in 0 s, and were filed as
  failed tasks — 45 in three hours, plus capped model-driven runs on other
  providers. They dragged the build, OpenCode and real rows (OpenCode
  read 36 vs 46 % on 22 vs 24 "runs"; 11 vs 13 were real).
- Numbers with those records dropped (report at 19:39, 1,938 runs):
  **provider group 113 vs 115, completed 88.5 vs 68.7 %, provider
  failures recovered 72.7 vs 23.4 %**, failures met 44 vs 47; real 383 vs
  385, 70.2 vs 67.0 %; build 94 vs 93, 86.2 vs 83.9 %, 33.0 vs 37.0 s/run;
  test 19.0 vs 27.8 s/run; OpenCode 11 vs 13, 72.7 vs 84.6 %. groq
  `switch_model` 52/52 since 05:30.
- OpenCode ask twin: 11 runs, FailEcho calls 0, failed or unfinished 3.
  Trigger 9 (6+) not fired.
- Onboarding since 16:54: gpt-oss-120b other pass, gpt-oss:20b clean pass,
  nemotron-3-nano clean pass; nex, lfm, Gemini provider 429s. The rotation
  is mixed-radix now, so the scene changes once per turn of all 11 models
  (~5.5 h): every run since 17:22 was "clean". By design, not a bug.
- Triggers 5, 6, 8, 9: nothing fired.

**Changed.** The scheduler skips a provider at the fleet's own daily cap
like a dead one; the report drops records of runs that never started
(`a5b1521`; `2a08d4b` makes it recognise old records, which carried no
answer). Tests, re-pinned between runs. **For you.** NVIDIA: the fleet
spent 413 calls today, most by OpenCode. Check build.nvidia.com credits;
if there is room I can raise the cap (their documented tier is 40 a
minute, far above it), otherwise OpenCode stays capped each afternoon.

### 20:31 UTC (18 Sep) — pass 34

**Checked.** All active incl. both lanes, no failed units, no tracebacks
or OOM. 587 MB available, disk 65%, no VM up. Production 788, all
first-party, independent 0.

**Found.**
- The cap skip works: 127 skips logged this hour, no 0-second "runs"
  filed. Groq (703 of 700), Ollama (152 of 150) and NVIDIA (413 of 400)
  are all at the fleet's own caps until the UTC day turns; only Mistral,
  xKiro, Gemini's remainder and the no-provider personas run tonight. The
  provider group will barely grow before 00:00.
- Provider group 119 vs 121: completed **89.1 vs 70.2 %**, recovered 72.7
  vs 23.4 %, failures met 44 vs 47. groq `switch_model` 54/54.
- Real 400 vs 402: 70.5 vs 67.2 %. Build 107 vs 106: 87.9 vs 85.8 %,
  33.2 vs 35.5 s/run. Test 18.7 vs 27.5 s/run. OpenCode unchanged
  (NVIDIA capped): 11 vs 13, ask twin 0 FailEcho calls.
- Onboarding 19:50 gemma4 clean pass; 20:21 ling 429.
- Triggers 5, 6, 8, 9: nothing fired.

**Changed.** Nothing. **For you.** Same as 19:31: NVIDIA credits. The
groq cap (700) is below its documented 1,000 requests a day; raising it
to 900 would keep groq's personas — the ones carrying the provider
result — running into the evening. Say if you want it.

### 21:31 UTC (18 Sep) — pass 35

**Checked.** All active incl. both lanes, no failed units, no tracebacks
or OOM. **296 MB available** with an OpenCode guest (768 MB) up — the
lowest reading so far; no kill, swap absorbing it. Disk 65%. One VM and
scratch file, the live run's. Production 801, all first-party,
independent 0.

**Found.**
- Caps raised at 21:00 (user: "max everything"): groq 732 calls today
  (its per-model token budget, not our cap, is what stops it now), NVIDIA
  467 of 2,400, xKiro 468 calls with token counting from 21:00 (15.6k so
  far on the new counter; the 1.01M spent before it is not in it, so
  xKiro can run over its tier once more tonight — it enforces its own).
- Provider group 129 vs 131: completed **89.9 vs 71.8 %**, recovered 73.3
  vs 22.9 %, failures met 45 vs 48. groq `switch_model` 55/55.
- OpenCode 14 vs 15: 71.4 vs 86.7 %; ask twin 0 FailEcho calls in 14
  runs, 4 failed or unfinished. Trigger 9 (6+) not fired.
- Real 419 vs 421: 70.2 vs 67.2 %. Build 118 vs 118: 89.0 vs 87.3 %,
  34.5 vs 37.8 s/run. Test 18.6 vs 27.4 s/run.
- Onboarding 21:12 ministral-14b clean pass; nemotron-super 5xx, ling 429.
- Triggers 5, 6, 8, 9: nothing fired.

**Changed.** Nothing in this pass. Between passes, user-directed: NVIDIA
cap 1,200 then every cap at the provider's stated tier, xKiro token cap,
tokens counted per provider, onboarding counted against the same tier,
VM lane every minute, onboarding every 20 minutes (`92c5958`,
`09f2576`). **For you.** Nothing new. If memory keeps under 300 MB with
the faster lanes, I will drop the OpenCode guest to one run in two VM
ticks rather than let the kernel choose what to kill.

### 23:45 UTC (18 Sep) — pass 36 (late: usage limit)

**Checked.** All active, no failed units now, no OOM. 433 MB available,
disk 65%. One VM and scratch file, the live run's. Production 830, all
first-party, independent 0.

**Found.**
- Light lane crashed once at 21:53: `KeyError: 'choices'` (a provider
  answered 200 with no completion). VM lane hit its 7 min start timeout
  once at 22:51–22:58 with no output; the next run was clean. Watching.
- groq hit its per-model token-per-day limit (200k) on both gpt-oss models
  at ~21:59; recovery switched models as designed.
- Provider group 151 vs 154: completed **90.7 vs 74.0 %**, recovered 73.5
  vs 23.1 %.
- OpenCode 25 vs 27: 72.0 vs 74.1 %; ask twin now **0.08 FailEcho calls
  per run** — its first calls (about 2 in 25 runs). Trigger 9 not fired.
- Real 462 vs 465: 69.0 vs 67.1 %. Build 144 vs 143: 86.1 vs 86.7 %
  (tie). Test ask 18.1 vs 26.5 s/run.
- Triggers 5, 6, 8, 9: nothing fired.

**Changed.** Chat replies without `choices` now raise an error the
recovery path handles, in fleet and onboarding; test (`e66ac47`).
Deployed, lab re-pinned between runs. **For you.** Nothing new.

### 00:14 UTC (19 Sep) — user-directed

OpenCode budget 240 → 380 s (inner clock 220 → 360 s), VM-lane start
timeout 420 → 540 s (`bcb5d0d`). The OpenCode versus row mixes old and new
runs from here; compare runs after 00:14 on their own. Watch memory: the
768 MB guest now lives up to 140 s longer.

### 00:30 UTC (19 Sep) — pass 37

**Checked.** All active, no failed units, no tracebacks, no OOM. 586 MB
available, disk 65%. One VM and scratch file, the live run's. Production
846, all first-party, independent 0. Canary last 04:16 yesterday, all ok.

**Found.**
- The 380 s OpenCode budget never took effect: `guest_init` caps every
  task at 300 s, killed the 00:27 ask run there, and returned no events
  (363 s, 0 steps). The same loss sits behind **14 of 41 runs for each
  twin** since 18 Sep: exit unknown, 0 steps, OpenCode ignoring the inner
  timeout's TERM until the guest killed it. Those runs were counted as
  failures with no steps; the versus row is noisier than it looked.
- The ledger's `timed_out` was always false (`getattr` on a dict).
- Provider group 162 vs 165: **90.7 vs 73.9 %**, recovered 72.5 vs 21.8
  %. OpenCode 29 vs 31: 69.0 vs 77.4 %, ask 0.07 FailEcho calls a run.
  Real 69.2 vs 66.9 %. Build 85.6 vs 86.2 % (tie).
- Onboarding 67 runs, 22 of 37 graded passed. Triggers 5, 6, 8, 9:
  nothing fired.

**Changed.** OpenCode budget 300 s (inner 280 s with `timeout -k 10`),
`timed_out` read from the result; test that the budget fits the guest
cap (`6640c9f`). Deployed, lab re-pinned between runs. **For you.**
360 s needs `MAX_TIMEOUT` raised in the guest, which means rebuilding the
1000 MB rootfs; say if you want that. For now it is 280 s, up from 220.

### 00:51 UTC (19 Sep) — user-directed: "make the product better for real use"

- MCP tool list: optional fields as their plain type instead of `anyOf
  [X, null]` + `default: null`. 6.4k to 5.5k chars, about 220 tokens off
  every turn of every MCP client; explicit nulls still accepted; tests
  (`5202ecd`). Live on production and the lab (0 `anyOf` in tools/list).
- The lab re-pin for this ran while a VM-lane builder run was in flight
  (the wait loop gave up). That run finished normally.
- First OpenCode run on the 280 s budget that ran long (blind, 284 s)
  kept its events: 3 steps recorded, not 0.
- Not done: raising the guest's 300 s cap (rootfs rebuild). It changes
  the harness, not the product; revisit if runs still end at 280 s.

### 01:31 UTC (19 Sep) — pass 38

**Checked.** All active, no failed units, no tracebacks, no OOM. 895 MB
available, disk 65%. No VM or scratch file left over. Production 863, all
first-party, independent 0.

**Found.**
- OpenCode since the 280 s fix: 11 runs, **every one recorded its steps**
  (3 to 11; none at 0). Ask 5 of 5 completed, blind 5 of 6. Three hit the
  280 s clock, and two of those had already written their result. Ask
  twin: 0 FailEcho calls in these 11 runs; none met a failure.
- Cumulative: provider group 176 vs 179, **91.5 vs 74.9 %**, recovered
  73.6 vs 22.4 %. OpenCode 34 vs 37: 73.5 vs 78.4 % (old runs included).
  Real 69.7 vs 66.9 %. Build 86.1 vs 86.6 % (tie).
- Onboarding 70 runs, 24 of 39 graded passed (+2 since pass 37, no new
  fails). Triggers 5, 6, 8, 9: nothing fired.

**Changed.** Nothing. **For you.** Nothing new.

### 02:23 UTC (19 Sep) — user-directed: publish prep ("I green light")

Outside Claude Code nothing gave a failing agent the network's answer
unless its model asked, and models rarely do (OpenCode 0.06 calls a run,
builders 8 asks in 40 failures); the lab's gains came from harness code a
user does not have. `failecho-autoreport` 0.1.4 adds opt-in advice
(`08f063a`), built and checked, not published. Setup page section held on
`docs-advise` (`0040583`). New twin pair `fleet-wrap-ask/blind` measures
the shipped path with no harness help (`817a8ce`), deployed, lab
re-pinned between runs. Checklist: `docs/publish-2026-09-21.md`.

### 02:37 UTC (19 Sep) — clean-install test of 0.1.4 (user asked, before publishing)

Wheel in a fresh venv, real httpbingo failures, advice from the lab. First
run: the 503's advice was lost at the 1.5 s budget (later runs ~0.2 s), and
a known 429 with evidence but no recommendation gave no line. Fixed: 3 s
budget, and an evidence line when there is no recommendation
(`3664559`). Rebuilt, `twine check` passes; three fresh runs all advised:
503 "try retry, worked 42/60", 429 "no clear fix yet; backoff 128/251,
retry 49/98"; FailEcho unreachable adds 0.06 s and nothing breaks. First
wrapped-pair runs (02:23, 02:24): both completed, no failures met yet. The
lab re-pin for this ran while a VM-lane run was starting (wait loop timed
out); the change was package-only.

### 02:38 UTC (19 Sep) — pass 39

**Checked.** All active, no failed units, no tracebacks, no host OOM. 581
MB available, disk 65%. One VM and scratch file, the live run's.
Production 874, all first-party, independent 0.

**Found.**
- OpenCode since 01:31: 10 runs, 8 completed. The ask twin **called
  `check_tool_failure` twice in one run** (02:12, the one run of five that
  met a failure) — its first calls on record in a run's tools. Trigger 9
  cannot fire while it asks.
- One OpenCode run (ask, 01:42) was killed (exit 137) at 43 s with 0 steps:
  not the clock, most likely the 768 MB guest running out. First since the
  memory raise; watching. The other two 137s were the 280 s `-k` kill.
- Provider group 191 vs 193: **92.1 vs 75.1 %**, recovered 74.5 vs 23.0 %.
  Real 70.2 vs 66.5 %. Build 85.7 vs 85.1 % (tie). OpenCode 74.4 vs 78.0 %.
  Wrapped pair: 1 run each, both done, no failures met yet.
- Onboarding 74 runs, 25 of 42 graded passed. Home scene: every run so far
  kept out of the home directory; trigger 5 not fired. Triggers 6, 8, 9:
  nothing fired.

**Changed.** Nothing this pass. **For you.** Publishing
`failecho-autoreport` 0.1.4 (tested, in `dist/`) is still yours.

### 03:34 UTC (19 Sep) — user-directed: MCP proxy, built and in the lab

`failecho-mcp proxy -- <server>` (`4485004`, `4e3ce2e`): every message
passes as the same bytes except a failed `tools/call`, which gets one
advice line; outcomes reported as shape only; retries inferred from a
repeated call (arguments hashed, in memory). 21 proxy tests; found and
fixed an abort on shutdown (SIGABRT instead of the server's exit code).
Official SDK client against server-filesystem and server-everything:
identical to direct in every field. Lab pair `fleet-ocp-ask-n/blind-n`
(`859a3f9`): OpenCode with a real-API MCP server (PyPI, npm, crates.io,
GitHub), through the proxy or not, no other difference. One manual run
each: both completed via `packages_pypi_latest` x3; the proxy in the guest
reported to the lab. Those two records went to /tmp/failecho-fleet (no
StateDirectory on a systemd-run); removed, the scoreboard counts timer runs
only. VM lane paused for the manual runs, restarted.

### 03:35 UTC (19 Sep) — pass 40

**Checked.** All active, no failed units, no tracebacks, no host OOM. 513
MB available, disk 66%. One VM and scratch file, the live run's.
Production 892: 884 first-party, **8 sparse agent rows**, independent 0.

**Found.**
- The 8 agent rows are mine: the 03:03 hand test of the MCP proxy with the
  SDK's `stdio_client`, which spawns servers with a minimal environment, so
  the exported lab endpoint and token never reached the proxy and it
  reported to production unlabelled (secure-filesystem-server, 4 rows;
  servers/everything, 4 rows; two throwaway reporter ids). Below the
  adoption threshold, so not counted as independent, but stored as agent.
  Not purged: that is a write to the production DB.
- OpenCode ask twin killed early (exit 137, 2 steps) at 02:43 and 03:15;
  early kills so far ask 3, blind 1 — the ask twin also runs an MCP
  client, and the 768 MB guest may be tight for it. Watching; it would
  bias the OpenCode row against the ask twin.
- The ask twin called FailEcho again (03:00: check, then
  report_recovery_outcome), and completed.
- Provider group 205 vs 207: **92.7 vs 75.8 %**, recovered 75.9 vs 24.6 %.
  Real 71.0 vs 66.4 %. Build 85.2 vs 85.2 %. OpenCode 42 vs 44: 71.4 vs
  77.3 %. Wrapped 3 vs 3, all done, no failures met. Proxy pair: no timer
  run yet. Onboarding 76 runs, 26 of 44 passed. Triggers: none fired.

**Changed.** CLAUDE.md: the third incident and the rule (pass `env=`
explicitly to anything a client library spawns). **For you.** Purge the 8
rows from production (a DB write, yours to approve), or let the 48 h
retention take them.

### 04:47 UTC (19 Sep) — pass 41

**Checked.** All active, no failed units, no tracebacks, no host OOM. 549
MB available, disk 66%. One VM and scratch file, the live run's.
Production 903: 895 first-party, the same 8 sparse rows, independent 0.
Checking the proxy by hand against lab.failecho.com/mcp and DeepWiki left
production's counts unchanged (env passed explicitly this time).

**Found.**
- Proxy pair (OpenCode through `failecho-mcp proxy` vs direct): 4 vs 4
  runs, all completed, every run used the `packages` tools; no tool
  failed yet, so no advice shown yet.
- Prod pair: first runs at 04:5x, after its slots passed once without a
  run at 04:22 (not explained; the next cycle ran both). prod-ask asked
  production on an NVIDIA timeout, got nothing, retried, recovered.
- OpenCode ask twin stalls: since 00:14, runs that ended with 0-1 steps
  and no result are ask 4, blind 1 (two exit 137, two at the 280 s clock
  with no step). The ask twin also starts a remote MCP client; if that is
  the cause it biases the OpenCode row (69.6 vs 79.2 %) against it.
  Watching; the ledger does not keep OpenCode's stderr, next step if it
  continues.
- Provider group 221 vs 222: **92.8 vs 76.1 %**, recovered 75.8 vs 23.5 %.
  Real 71.2 vs 66.3 %. Build 85.1 vs 85.1 %. Wrapped 5 vs 5, all done.
  Onboarding 79 runs, 28 of 46 passed. Triggers: none fired.

**Changed (between passes, user-directed).** Prod pair (`c5d759b`);
proxy for remote Streamable HTTP servers (`4cfd7b1`). **For you.**
Publish `failecho-autoreport` 0.1.4 (needs your PyPI token); the 8
production rows (purge or let expire).

### 04:58 UTC (19 Sep) — user-approved purge of the 8 production rows

User said yes to removing them. Backup first:
`/srv/failecho/backups/pre-purge-8-rows-20260919T045850Z.db` (600,
failecho). Deleted in one transaction, by id and service: observations
871-878 (source agent; secure-filesystem-server and servers/everything)
and their 4 fingerprints (observation_count 1, those services only). No
hourly or recovery rows referenced them. Afterwards: 0 agent rows, 0
fingerprints for those services. Not undone: the few query counters those
failures' advice reads bumped (integers, not attributable).

### 05:0x UTC (19 Sep) — failecho-autoreport 0.1.4 on PyPI

Published by the user. Installed from PyPI in a clean venv: the module is
byte-identical to the repo source; against the lab, advice on both real
failures in both runs (503 "try retry, worked 62/93"; 429 evidence line),
unreachable FailEcho adds 0.08-0.23 s, all reports sent. First cold read
2.46 s, inside the 3 s budget. Setup page advice section live
(`b6d6dba`). Production after the purge: sparse 0, independent 0.

### 05:39 UTC (19 Sep) — user-directed: proxy ported to npm, 0.2.0 built

Node proxy (`npm-relay/bin/proxy.js`, zero deps), same behaviour as the
Python one; the proxy tests run on both (50), plus a cross-language test
on real error strings for identical classes and advice lines. Against
server-filesystem and server-everything through the Node proxy: identical
to direct in every field; production unchanged (sparse 0, independent 0).
failecho-mcp 0.2.0 built for PyPI and npm, installed clean from each,
relay and proxy modes both work (`bec5b14`). Setup section held on
`docs-proxy`. Publishing is the user's; steps in
`docs/publish-2026-09-21.md`.

### 05:40 UTC (19 Sep) — pass 42

**Checked.** All active, no failed units, no tracebacks, no host OOM. 540
MB available, disk 66%. No VM or scratch left over. Production 907, all
first-party, sparse 0, independent 0.

**Found.**
- Proxy pair (OpenCode through `failecho-mcp proxy` vs direct, 7 vs 7):
  the proxied twin hit the 280 s clock in 3 runs (2 after writing its
  result; completed 85.7 vs 100 %), the direct twin in 0; its runs are
  slower on the whole (57-363 s vs 40-375 s). Both proxies exit within
  0.02 s on EOF, TERM and INT with no child left, so not a shutdown hang
  as tested. No tool has failed yet in the pair, so no advice shown.
- The Python proxy exited 241/254 when its server died of a signal
  (negative code to os._exit).
- Prod pair 2 vs 2, all done; each met one NVIDIA timeout and recovered.
- Provider group 230 vs 231: **93.0 vs 74.9 %**, recovered 77.9 vs 21.9 %.
  Real 71.7 vs 66.1 %. Build 85.1 vs 84.6 %. OpenCode 70.8 vs 80.0 %
  (the ask twin called FailEcho twice at 05:29). Wrapped 7 vs 7, all done.
  Onboarding 82 runs, 30 of 48 passed. Triggers: none fired.

**Changed.** Signal exits 128+n; OpenCode runs record how long OpenCode
ran after writing its result (`ee5f5ea`), to tell a stuck process from a
slow model. Deployed, lab re-pinned between runs (timers paused), 0.2.0
wheel rebuilt with the fix. **For you.** Publish failecho-mcp 0.2.0 (PyPI
and npm) — or wait a few hours for the idle-time numbers on the proxy
pair first; I would wait.

### 06:25 UTC (19 Sep) — failecho-mcp 0.2.0 on PyPI; npm not yet

PyPI 0.2.0 installed clean: `proxy.py` identical to source, pulls
failecho-autoreport 0.1.4; relay and proxy modes work against the lab and
server-filesystem; production unchanged (sparse 0, independent 0). npm
still lists 0.1.0 and 0.1.1 only, so the setup page's proxy section leads
with `uvx` and does not mention npx yet (`d4459cc`, live).

### 06:31 UTC (19 Sep) — pass 43

**Checked.** All active, no failed units, no tracebacks, no host OOM. 513
MB available, disk 66%. One VM and scratch file, the live run's.
Production 910, all first-party, sparse 0, independent 0.

**Found.**
- failecho-mcp 0.2.0 is on npm too (user). Via `npx` from a clean cache:
  relay and proxy modes work, `proxy.js` identical to source; production
  unchanged. `npx` line added to /setup.
- Proxy pair now 9 vs 9, **88.9 vs 88.9 %**: this hour the direct twin hit
  the clock (05:57, 1 step) and the proxied one did not. Idle time after
  the result: proxied 5-8 s, direct 89-134 s. The proxy is not what kept
  OpenCode running; the earlier gap was the provider. No tool has failed
  in the pair yet, so no advice shown.
- Prod pair 4 vs 4: 75 vs 100 %; prod-ask's NVIDIA timeout got no advice
  from production and its retry failed.
- Provider group 241 vs 242: **92.9 vs 74.8 %**, recovered 78.4 vs 23.1 %.
  Real 72.1 vs 65.9 %. Build 85.2 vs 83.7 %. OpenCode 70.0 vs 80.8 %.
  Onboarding 83 runs, 30 of 49 passed. Triggers: none fired.

**Changed.** /setup names `npx` for the proxy. **For you.** Nothing.

### 07:44 UTC (19 Sep) — pass 44

**Checked.** All active, no failed units, no tracebacks, no host OOM. 533
MB available, disk 66%. One VM and scratch file, the live run's. Canary
04:18 all ok. Production 920, all first-party, sparse 0, independent 0.

**Found.**
- Wrapped pair: the ask twin met GitHub 403s twice (06:56, 07:27) and the
  wrapper attached the network's answer to 4 failures (no recommendation
  for a 403 without a token, so the evidence line or none); still 100 vs
  100 % over 11 runs each.
- Proxy pair 12 vs 12: **91.7 vs 75.0 %**; the direct twin hit the clock
  twice (06:40, 07:04). Idle after result: proxied 8-22 s, direct 53 s.
  Trigger 10 not fired. Still no tool failure inside the pair, so no
  advice line in a real OpenCode run yet.
- Prod pair 6 vs 6: 83.3 vs 100 %; prod-ask met 403s and NVIDIA timeouts,
  production had no recommendation for either.
- Provider group 255 vs 255: **92.9 vs 74.1 %**, recovered 80.0 vs 24.4 %.
  Real 72.7 vs 65.7 %. Build 83.6 vs 82.1 %. OpenCode 72.2 vs 81.8 %.
  Onboarding 84 runs, 31 of 50 passed. Triggers 5, 6, 8, 9, 10: none.

**Changed.** Nothing. **For you.** Nothing.

### 08:44 UTC (19 Sep) — pass 45

**Checked.** All active, no failed units, no tracebacks, no host OOM. 576
MB available, disk 66%. One VM and scratch file, the live run's.
Production 930, all first-party, sparse 0, independent 0.

**Found.**
- Proxy pair 16 vs 16: 87.5 vs 75.0 %. Proxied twin idled over 60 s after
  its result twice (07:49 185 s, 08:42 239 s); trigger 10 needs 3 and the
  direct twin has one (89 s), so not fired. The unpaired OpenCode twins
  idle that long too (81-226 s), so it is not clearly the proxy.
- 08:27 proxied run: `pypi_latest` called 7 times, every version written
  as "unknown", and the lab received no report at all from its proxy
  (other runs' proxies do report). Two proxy-pair runs ended 137 (07:49
  proxied, 08:11 direct). Cause not visible from the host.
- The only tool failures the proxy has reported so far are GitHub 403s
  (06:58, 3); the network had no advice for a new service, so no line.
- Provider group 269 vs 269: **92.6 vs 73.6 %**, recovered 79.5 vs 23.9 %.
  Real 73.1 vs 65.7 %. Build 84.3 vs 82.5 %. OpenCode 73.7 vs 81.4 %.
  Wrapped 13 vs 13, 100 %. Prod 8 vs 8: 87.5 vs 100 %. Onboarding 87
  runs, 33 of 53 passed. Triggers: none fired.

**Changed.** OpenCode runs now keep MCP log lines and the guest's
OOM-kill count (`2e68f5b`); deployed, lab re-pinned with timers paused.
**For you.** Nothing yet; if the next runs show the proxy dropping calls,
that is a 0.2.1.

### 09:44 UTC (19 Sep) — pass 46

**Checked.** All active, no failed units, no tracebacks, no host OOM. 663
MB available, disk 66%. One VM and scratch file, the live run's.
Production 946, all first-party, sparse 0, independent 0.

**Found.**
- New diagnostics working: guest OOM kills 0 in every OpenCode run this
  hour, including both 137s (09:28 direct, 09:40 unpaired ask), which came
  at ~294 s: the `timeout -k` kill after OpenCode ignored TERM, not memory.
  OpenCode's stderr carried no MCP lines in any run.
- Proxy pair 20 vs 20: **90.0 vs 80.0 %**; all 5 proxied runs this hour
  completed and every tool call went through; the 08:27 dropped-calls run
  has not recurred. Idle after result: proxied 8-52 s, direct up to 248 s.
  Trigger 10 not fired. Still no tool failure in the pair this hour.
- Provider group 279 vs 279: **92.8 vs 73.8 %**, recovered 80.0 vs 23.4 %.
  Real 73.3 vs 66.1 %. Build 85.0 vs 82.8 %. OpenCode 75.4 vs 82.5 %.
  Wrapped 15 vs 15, 100 %. Prod 10 vs 10: 80 vs 100 %. Onboarding 89
  runs, 34 of 54 passed. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 10:44 UTC (19 Sep) — pass 47

**Checked.** All active, no failed units, no tracebacks, no host OOM. 580
MB available, disk 66%. One VM and scratch file, the live run's.
Production 955, all first-party, sparse 0, independent 0.

**Found.**
- OpenCode ask twin (remote FailEcho MCP server) stalled again, 09:58 (0
  steps) and 10:21 (1 step): **6 stalls since midnight vs 1 for the blind
  twin**, no guest OOM, nothing in stderr. Its one difference is the remote
  MCP connection, so it is probably the cause, and it drags the OpenCode
  row (73.4 vs 83.3 %) for a harness reason, not a product one.
- Proxy pair 23 vs 23: **91.3 vs 82.6 %**; every proxied run completed,
  idle 43-51 s; trigger 10 not fired. No tool failure in the pair yet.
- Provider group 290 vs 290: **93.1 vs 74.8 %**, recovered 80.0 vs 23.4 %.
  Real 73.9 vs 66.7 %. Build 85.4 vs 82.9 %. Wrapped 17 vs 17, 100 %. Prod
  12 vs 12: 83.3 vs 100 %. Onboarding 90 runs, 34 of 55. Triggers: none.

**Changed.** The diagnostic now reads OpenCode's own log file
(~/.local/share/opencode/log), checked with `sh -n` and a dry run
(`b6602f9`); deployed, lab re-pinned with timers paused. **For you.**
Nothing yet; the next stall should say why.

### 11:44 UTC (19 Sep) — pass 48

**Checked.** All active, no failed units, no tracebacks, no host OOM. 475
MB available, disk 66%. One VM and scratch file, the live run's.
Production 973, all first-party, sparse 0, independent 0.

**Found.**
- The log-file diagnostic works: it caught NVIDIA's "Service temporarily
  overloaded" (503 stream error) inside an OpenCode run at 11:24.
- OpenCode ask twin: one short run (11:01, 1 step, exit 0, no result) with
  **no MCP, error or timeout line** in OpenCode's log — the model stopped,
  nothing hung. So at least this "stall" is not the remote MCP connection.
- Guest OOM now visible: 2 runs ended 137 with an OOM kill (11:20 proxied,
  11:26 unpaired blind), both after their result was written; one per
  side, so no bias. 768 MB is tight but not yet costing tasks.
- Proxy pair 28 vs 28: **92.9 vs 85.7 %**; idle proxied 10-86 s, direct
  54-105 s; trigger 10 not fired. No tool failure in the pair yet.
- Wrapped ask twin: 403s again, advice attached to 6 failures so far.
- Provider group 301 vs 300: **92.7 vs 75.0 %**, recovered 78.3 vs 22.9 %.
  Real 73.9 vs 67.1 %. Build 85.7 vs 82.9 %. OpenCode 73.9 vs 84.5 %.
  Prod 15 vs 15: 86.7 vs 100 %. Onboarding 92 runs, 36 of 57. Triggers:
  none fired.

**Changed.** Nothing. **For you.** Nothing.

### 12:44 UTC (19 Sep) — pass 49

**Checked.** All active, no failed units, no tracebacks, no host OOM. 356
MB available (an OpenCode guest up), disk 66%. One VM and scratch file,
the live run's. Production 980, all first-party, sparse 0, independent 0.

**Found.**
- Guest OOM kills since 08:50: FailEcho-side OpenCode twins 4 (oc-ask 2,
  ocp-ask 2), blind twins 1, over ~31 runs a side; the 2 kills that cost
  the task were both FailEcho-side (12:20 ocp-ask, 12:39 oc-ask). Those
  twins run one more process (MCP client or proxy) in 768 MB: a harness
  bias against the product.
- Proxy pair 31 vs 32: 90.3 vs 84.4 %. Still no tool failure inside the
  pair. Trigger 10 not fired (proxied idle 41-70 s).
- Provider group 313 vs 313: **93.0 vs 74.8 %**, recovered 78.9 vs 22.0 %.
  Real 74.6 vs 67.2 %. Build 85.0 vs 83.0 %. OpenCode 74.0 vs 84.0 %.
  Wrapped 22 vs 22, 100 %. Prod 17 vs 17: 88.2 vs 100 %. Onboarding 94
  runs, 37 of 58. Triggers: none fired.

**Changed.** Runs the guest killed for memory before their result are
excluded from every rate and counted on their own row per side
(`f429675`); preview: OpenCode 75.0 vs 84.0 %, proxy pair 93.5 vs 84.4 %,
1 lost run a side excluded. The guest was not enlarged (host headroom,
infra note). Deployed, lab re-pinned with timers paused. **For you.**
Nothing.

### 13:44 UTC (19 Sep) — pass 50

**Checked.** All active, no failed units, no tracebacks, no host OOM. 541
MB available, disk 66%. One VM and scratch file, the live run's.
Production 990, all first-party, sparse 0, independent 0.

**Found.**
- Proxy pair 34 vs 35: **94.1 vs 82.9 %** (1 FailEcho-side run lost to
  guest memory, excluded). Proxied twin idled over 60 s after its result
  3 times this hour (74-159 s), but the direct twin did too (73, 97 s) and
  so do the unpaired twins: OpenCode behaviour, not the proxy. Trigger 10
  not fired. Still no tool failure inside the pair.
- OpenCode (MCP tools) 74 vs 79: 75.7 vs 83.5 %; one more FailEcho-side
  memory kill (13:12), now 2 excluded vs 0.
- Onboarding quiet since 12:33: ollama and openrouter at their daily
  caps, so their models' slots are skipped, as designed.
- Provider group 324 vs 322: **92.6 vs 74.5 %**, recovered 78.4 vs 21.4 %.
  Real 74.5 vs 67.2 %. Build 85.0 vs 83.2 %. Wrapped 24 vs 24, 100 %.
  Prod 19 vs 19: 89.5 vs 100 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 14:44 UTC (19 Sep) — pass 51

**Checked.** All active, no failed units, no tracebacks, no host OOM. 442
MB available, disk 66%. One VM and scratch file, the live run's.
Production 1013, all first-party, sparse 0, independent 0. Onboarding
still capped (ollama, openrouter).

**Found.**
- First tool failure inside the proxy pair: `packages/github_stars` 403 at
  14:37, proxied run. No advice line, correctly: the network has 6 reports
  of that shape and no recovery evidence for it. **Product gap:** the same
  GitHub 403 has plenty of evidence under `api.github.com` (HTTP-level
  callers), but the proxy names the service by the MCP server
  (`packages`), so an MCP server wrapping an API never meets the API's own
  evidence. Real MCP servers (github-mcp-server and the like) will split
  the same way. Not a quick fix; for the report and the next version.
- Proxy pair 39 vs 40: **94.9 vs 85.0 %**; idle >60 s on both sides again
  (proxied 69-141, direct 65-253); trigger 10 not fired.
- OpenCode (MCP tools) 76.9 vs 84.3 %, 3 FailEcho-side runs excluded for
  guest memory vs 0. Wrapped: advice on 8 failures, 100 vs 100 %.
- Provider group 332 vs 330: **92.8 vs 75.2 %**, recovered 78.4 vs 22.1 %.
  Real 74.7 vs 67.4 %. Build 84.9 vs 83.1 %. Prod 22 vs 22: 90.9 vs 100 %.
  Triggers: none fired.

**Changed.** Nothing. **For you.** The naming gap above is a product
decision (e.g. let the proxy learn the upstream host, or map MCP servers
to the APIs they wrap); worth a word before the launch post describes the
proxy.

### 15:44 UTC (19 Sep) — pass 52

**Checked.** All active, no failed units, no tracebacks, no host OOM. 351
MB available (a guest up), disk 66%. One VM and scratch file, the live
run's. Production 1022, all first-party, sparse 0, independent 0.
Onboarding running again (15:32).

**Found.**
- Proxy pair 43 vs 44: 93.0 vs 86.4 %. The one proxied failure this hour
  (15:31, exit 1, 0 steps) was NVIDIA: 429 "Too Many Requests" three
  times, then 503 "Service temporarily overloaded", from OpenCode's own
  log — the provider, not the proxy. Two proxied runs OOM-killed after
  their result (no task lost). Idle >60 s: proxied 2 (161, 61), direct 0
  this hour; trigger 10 needs 3 — watching.
- OpenCode (MCP tools) 75.9 vs 83.0 %, 3 vs 0 excluded for guest memory.
  Wrapped 30 vs 30, 100 %. Prod 25 vs 25: 92.0 vs 100 %.
- Provider group 340 vs 337: **92.6 vs 75.7 %**, recovered 77.7 vs 22.9 %.
  Real 74.7 vs 67.4 %. Build 85.2 vs 82.6 %. Onboarding 96 runs, 38 of 59.
  Triggers: none fired.

**Changed.** Nothing. **For you.** Still open from pass 51: the MCP
server vs API naming gap (fix now, or a known limit for the launch post).

### 16:44 UTC (19 Sep) — pass 53

**Checked.** All active, no failed units, no tracebacks, no host OOM. 505
MB available, disk 66%. One VM and scratch file, the live run's.
Production 1035, all first-party, sparse 0, independent 0.

**Found.**
- Trigger 10's letter was met in a two-hour window (proxied twin idled
  >60 s after its result 4 times, 14:53-16:42; direct 0), but not over the
  whole record: idle after result, all runs since 06:25 — proxied median
  44 s, mean 62, >60 s in 14 of 39; direct median 41 s, mean 62, >60 s in
  12 of 33. Same distribution; the window was chance. **Not fired.**
- Proxy pair 47 vs 48: **93.6 vs 83.3 %** (direct twin hit the clock twice
  this hour). Three more github_stars 403s through the proxy (16:38-39);
  still no recovery evidence for `packages`, so no advice line — the
  naming gap from pass 51.
- OpenCode (MCP tools) 75.3 vs 82.4 %, 4 vs 0 excluded for guest memory.
  Wrapped 33 vs 33, 100 %, advice on 11 failures. Prod 27 vs 27: 92.6 vs
  100 %.
- Provider group 350 vs 346: **92.3 vs 75.7 %**, recovered 77.6 vs 22.4 %.
  Real 74.3 vs 67.4 %. Build 85.5 vs 83.0 %. Onboarding 97 runs, 38 of 59.
  Triggers: none fired.

**Changed.** Nothing. **For you.** Still open: the naming gap (pass 51).

### 17:44 UTC (19 Sep) — pass 54

**Checked.** All active, no failed units, no tracebacks, no host OOM. 607
MB available, disk 66%. One VM and scratch file, the live run's.
Production 1043, all first-party, sparse 0, independent 0. Onboarding
capped again (ollama, openrouter) since 16:17, as designed.

**Found.**
- Proxy pair 50 vs 51: **94.0 vs 82.4 %**. Idle after result, all runs:
  proxied median 45.5 s (16 of 42 over 60), direct 41 s (12 of 35): same
  distribution; trigger 10 not fired. No advice line yet (naming gap).
- Wrapped 36 vs 36, 100 %, advice on 15 failures. Prod 31 vs 31: 90.3 vs
  96.8 %. OpenCode (MCP tools) 75.0 vs 83.0 %, 4 vs 0 excluded for memory.
- Provider group 356 vs 352: **92.4 vs 76.1 %**, recovered 77.6 vs 22.4 %.
  Real 73.8 vs 67.2 %. Build 84.3 vs 82.2 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Still open: the naming gap (pass 51).

### 18:07 UTC (19 Sep) — user-directed ("try it"): the naming gap

Proxy reports keep the MCP server's name; when that name has no advice the
proxy asks under the API host the tool wraps (`--upstream`, or built in for
the GitHub MCP server) and labels the line with the host. advice_text now
also uses pooled service evidence. Both languages, 58 proxy tests
(`53238cd`); lab's proxied OpenCode twin declares its tools' hosts;
deployed, lab re-pinned with timers paused. Real lab evidence gives, for a
GitHub 403 on a tool the network has never seen: "FailEcho (evidence from
api.github.com): no clear fix yet; on this service's other operations,
agents tried wait_until_reset worked 2/11, backoff worked 104/2330, ..."
(before: no line). autoreport 0.1.5 and failecho-mcp 0.2.1 built, clean
installs pass; not published (user's step).

### 18:30 UTC (19 Sep) — autoreport 0.1.5 and failecho-mcp 0.2.1 live

Published by the user. From PyPI (after its index caught up) and npm, in
clean installs: code identical to source; relay and proxy modes work. A
real GitHub 403 hit during the check, and the PyPI build answered it with
"FailEcho (evidence from api.github.com): no clear fix yet; on this
service's other operations, agents tried wait_until_reset worked 2/11,
backoff worked 104/2330 ..." — the first advice line from the proxy on a
real failure. `--upstream` added to /setup. Production unchanged (sparse
0, independent 0).

### 18:44 UTC (19 Sep) — pass 55

**Checked.** All active, no failed units, no tracebacks, no host OOM. 645
MB available, disk 67%. No VM or scratch left over. Production 1055, all
first-party, sparse 0, independent 0.

**Found.**
- VM lane idle since ~17:50 by design: NVIDIA at its daily cap (2404 of
  2400 calls), xKiro at its token cap (958k of 950k), groq out of quota
  (18:41). Builders, OpenCode and the proxy pair resume at 00:00 UTC; the
  prod pair (NVIDIA) too. Onboarding still capped (ollama, openrouter).
- Proxy pair 51 vs 52: 92.2 vs 82.7 %. Wrapped 39 vs 39, 100 %, advice on
  15. Prod 32 vs 31: 90.6 vs 96.8 %. OpenCode (MCP tools) 75.3 vs 83.2 %.
- Provider group 362 vs 358: **92.3 vs 76.3 %**, recovered 77.1 vs 22.2 %.
  Real 73.4 vs 66.8 %. Build 83.9 vs 82.3 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 19:44 UTC (19 Sep) — pass 56

**Checked.** All active, no failed units, no tracebacks, no host OOM. 612
MB available, disk 67%. No VM or scratch left over. Production 1080, all
first-party, sparse 0, independent 0.

**Found.** VM lane still capped (NVIDIA, xKiro) until 00:00 UTC; light
lane running. Wrapped 43 vs 44, 100 %, advice on 19 failures. Provider
group 367 vs 363: **92.4 vs 76.6 %**, recovered 77.1 vs 22.2 %. Real
73.1 vs 66.5 %. Other groups unchanged (VM-lane). Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 20:44 UTC (19 Sep) — pass 57

**Checked.** All active, no failed units, no tracebacks, no host OOM. 501
MB available, disk 67%. One VM and scratch file, the live run's (groq back:
its builders run again). Production 1102, all first-party, sparse 0,
independent 0.

**Found.** NVIDIA and xKiro still capped until 00:00 UTC (OpenCode, proxy
and prod pairs paused). Wrapped 48 vs 48, 100 %, advice on 27 failures.
Provider group 373 vs 368: **92.5 vs 76.6 %**, recovered 77.5 vs 22.7 %.
Real 72.6 vs 66.0 %. Build 84.1 vs 82.3 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 21:44 UTC (19 Sep) — pass 58

**Checked.** All active, no failed units, no tracebacks, no host OOM. 446
MB available, disk 67%. No VM or scratch left over. Production 1112, all
first-party, sparse 0, independent 0. Commits from here are authored with
the address the user named for the Fuyuki0 account.

**Found.** NVIDIA and xKiro still capped until 00:00 UTC. Wrapped 52 vs 52,
100 %. Provider group 377 vs 373: **92.6 vs 76.7 %**, recovered 77.5 vs
22.5 %. Real 72.0 vs 65.8 %. Build 84.0 vs 82.6 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 00:44 UTC (20 Sep) — passes 59 and 60 (two ticks, one entry)

**Checked.** All active, no failed units, no tracebacks, no host OOM. 419
MB available, disk 67%. One VM and scratch file, the live run's.
Production 1167, all first-party, sparse 0, independent 0. Canary last ran
04:18 on 19 Sep (daily timer), all steps ok.

**Found.**
- Caps reset at 00:00: the VM lane is running again (NVIDIA 90 calls so
  far today); groq still marked out of quota from 23:17, which expires on
  its own. Onboarding running (00:34, 103 runs, 41 of 63 passed).
- Proxy pair 53 vs 54: 90.6 vs 83.3 %. Four runs each since midnight, one
  proxied run hit the clock; no tool failure met, so no advice line yet in
  a scheduled run.
- OpenCode (MCP tools) 91 vs 97: 75.8 vs 83.5 %; one FailEcho-side memory
  kill at 00:07, after its result (task kept).
- Wrapped 63 vs 63, 100 %, advice on 31 failures. Prod 33 vs 32: 90.9 vs
  96.9 %.
- Provider group 397 vs 393: **92.7 vs 77.1 %**, recovered 77.9 vs 22.6 %.
  Real 70.8 vs 64.5 %. Build 84.1 vs 83.1 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing; the report refresh runs at
09:47 UTC today.

### 01:44 UTC (20 Sep) — pass 61

**Checked.** All active, no failed units, no tracebacks, no host OOM. 289
MB available with a guest up, disk 67%. One VM and scratch file, the live
run's. Production 1190, all first-party, sparse 0, independent 0. No
provider marked dead.

**Found.** Proxy pair 56 vs 57: 91.1 vs 84.2 %; one proxied run OOM-killed
after its result (task kept); still no tool failure met, so no advice line
in a scheduled run yet. Wrapped 64 vs 64, 100 %, advice on 32 failures.
Prod 35 vs 34: 91.4 vs 97.1 %. OpenCode (MCP tools) 76.6 vs 83.2 %.
Provider group 410 vs 406: **92.9 vs 77.3 %**, recovered 78.3 vs 22.2 %.
Real 70.9 vs 64.7 %. Build 84.1 vs 83.5 %. Onboarding 106 runs, 42 of 65.
Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 02:44 UTC (20 Sep) — pass 62

**Checked.** All active, no failed units, no tracebacks, no host OOM. 437
MB available, disk 67%. One VM and scratch file, the live run's.
Production 1203, all first-party, sparse 0, independent 0.

**Found.** Proxy pair 59 vs 60: 89.8 vs 85.0 %; two proxied runs hit the
280 s clock (02:34 lost its task). Idle after result over 60 s on both
sides again (proxied 97-218, direct 88-147), so trigger 10 stays unfired.
No tool failure met in the pair this hour. Wrapped 66 vs 66, 100 %,
advice on 32. Prod 37 vs 36: 91.9 vs 97.2 %. OpenCode (MCP tools) 77.3 vs
83.5 %. Provider group 423 vs 419: **93.1 vs 76.8 %**, recovered 79.2 vs
22.0 %. Real 71.3 vs 64.7 %. Build 84.0 vs 83.6 %. Onboarding 109 runs,
45 of 68. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 03:44 UTC (20 Sep) — pass 63

**Checked.** All active, no failed units, no tracebacks, no guest or host
OOM kill in the last 45 min. Disk 67%. One VM and scratch file, the live
run's. Production 1209, all first-party, sparse 0, independent 0.

**Found.**
- Memory dipped to **201 MB available** during a VM-lane run (464 MB a
  minute later). Largest users: this Claude session's processes (~590 MB
  across three), firecracker 252 MB. **Swap is 3.0 of 4.0 GB used** and
  slowly rising: worth watching, since production itself is ~70 MB.
- Proxy pair 62 vs 63: 90.3 vs 84.1 %; no tool failure met again, so still
  no advice line in a scheduled run. Wrapped 68 vs 68, 100 %. Prod 39 vs
  38: 92.3 vs 97.4 %. OpenCode (MCP tools) 78.0 vs 84.0 %.
- Provider group 437 vs 433: **92.9 vs 76.9 %**, recovered 79.2 vs 22.0 %.
  Real 71.4 vs 64.9 %. Build 83.5 vs 83.5 %. Onboarding 112 runs, 47 of
  70. Triggers: none fired.

**Changed.** Nothing. **For you.** If swap keeps filling, the cheap fix is
one OpenCode run every second VM tick instead of every tick; say if you
want that now rather than after the launch.

### 04:44 UTC (20 Sep) — pass 64

**Checked.** All timers and services active; canary green (04:17).
Production 1217, all first-party, sparse 0, independent 0. Disk 67%.

**Found and fixed.**
- `failecho-cloudflare-ips.service` **failed** — and had failed weekly
  since 13 Sep — on `yes: standard output: Broken pipe`: ufw stops reading
  stdin once it has its answer, so `yes` took SIGPIPE and pipefail killed
  the run. The rules themselves were intact (20 present). Fixed with
  `ufw --force delete`, dry-run against a fake ufw, installed, re-run:
  Cloudflare's list has grown to **22 ranges**, now applied; failecho.com,
  /setup, /v1/stats and the lab all 200 after (`381119c`).
- **Swap 3.6 of 4.0 GB** with 201-344 MB free, and one VM-lane run killed
  at the unit's 9 min timeout. OpenCode guests (768 MB each) ran every VM
  tick. They now wait **10 minutes between runs**, giving the slot to the
  next persona; both twins are slowed equally, so the comparison holds.
  After the change: swap 2.8 GB.
- Proxy pair 64 vs 64: 87.5 vs 84.4 %. Wrapped 70 vs 70, 100 %, advice on
  34. Prod 41 vs 40: 90.2 vs 97.5 %. Provider group 451 vs 447: **92.7 vs
  76.3 %**, recovered 78.8 vs 21.6 %. Real 71.5 vs 64.6 %. Build 83.5 vs
  83.5 %. Onboarding 114 runs, 47 of 72. Triggers: none fired.

**Changed.** Firewall script and the OpenCode gap (`381119c`); deployed,
lab re-pinned with timers paused. **For you.** Nothing.

### 06:45 UTC (20 Sep) — pass 65

**Checked.** All active, no failed units, no tracebacks, no OOM kills. 465
MB available, swap 3.25 of 4 GB (3.6 before yesterday's change, 2.8 just
after; watching). Disk 67%. One VM and scratch file, the live run's.
Production 1247, all first-party, sparse 0, independent 0.

**Found.**
- The OpenCode gap is doing its job: 43 slots handed to another persona
  since 04:44, and the VM lane has not hit its unit timeout since.
- Renamed metrics are live in the scoreboard: "runs marked completed (see
  grading)" and "retries the network said to skip" (314 test, 238 real).
- Provider group 473 vs 470: **92.4 vs 76.4 %** marked completed,
  recovered 78.1 vs 20.9 %, tokens 1806 vs 1967. Real 71.7 vs 64.9 %.
  Build 84.3 vs 84.0 %. OpenCode 79.2 vs 83.9 %. Proxy 65 vs 65: 86.2 vs
  84.6 %, no advice line yet. Wrapped 74 vs 74, 100 %, advice on 34.
  Prod 45 vs 44: 88.9 vs 97.7 %. Onboarding 118 runs, 51 of 76.
- Triggers: none fired.

**Changed.** Nothing this pass (the review fixes went in at 06:0x,
`e15404c`). **For you.** Two package releases carry those fixes
(autoreport 0.1.6, failecho-mcp 0.2.2); say when you want them built.

### 07:44 UTC (20 Sep) — pass 66

**Checked.** All active, no failed units, no tracebacks, no OOM kills. 345
MB available, swap 3.1 of 4 GB. Production 1269, all first-party, sparse
0, independent 0. No VM or scratch left over.

**Found.** Disk had crept 67 to 69 %: the journal at 2.3 GB and npm's
cache at 2.9 GB (mostly my own package tests). Cleared the npm cache (2.9
GB to 459 MB); the journal is already inside its own limit, so vacuuming
freed nothing. Disk now **64 %**, 21 GB free.
Provider group 483 vs 481: **92.5 vs 76.7 %** marked completed, recovered
78.3 vs 20.7 %. Real 71.9 vs 65.2 %. Build 84.0 vs 83.0 %. OpenCode 77.8
vs 82.6 %. Proxy 65 vs 65 (the gap slows it): 86.2 vs 84.6 %. Wrapped 77
vs 77, 100 %. Prod 48 vs 47: 89.6 vs 97.9 %. Onboarding 120 runs, 52 of
77. Triggers: none fired.

**Changed.** Nothing in the repo. **For you.** Still open: build autoreport
0.1.6 and failecho-mcp 0.2.2 with the review fixes.

### 10:46 UTC (20 Sep) — passes 67 and 68

**Checked.** All active, no failed units, no tracebacks. 463 MB available,
disk 68%. One VM and scratch file, the live run's. Production 1305, all
first-party, sparse 0, independent 0. Canary 04:17 green.

**Found and fixed.**
- **The host ran out of memory twice** (08:18, 08:31): the kernel killed
  firecracker, i.e. the test VM, not the server, but swap stood at 3.9 of
  4.0 GB and the next victim could have been production. Added **2 GB of
  swap** (`/swapfile2`, in fstab, fstab backed up) and widened the
  OpenCode gap from 10 to **30 minutes**. Since then: no OOM kill, swap
  3.9 of 6.1 GB with 2.2 free, and the VM lane has not timed out.
- One proxy test failed during the thrash and passes on its own; full
  suite re-run after the change.
- OpenCode runs lost to guest memory, excluded from the rates: 7
  FailEcho-side vs 0 blind (proxy pair 1 vs 0). The imbalance is the extra
  process in the same 768 MB guest, as before.
- Provider group 516 vs 511: **92.8 vs 77.7 %** marked completed,
  recovered 78.2 vs 20.4 %. Real 72.7 vs 66.3 %. Build 84.8 vs 83.8 %.
  OpenCode 77.9 vs 82.6 %. Proxy 65 vs 65 (30 min gap): 86.2 vs 84.6 %.
  Wrapped 84 vs 84, 100 %, advice on 38. Prod 55 vs 54: 90.9 vs 98.1 %.
  Onboarding 127 runs, 54 of 81. Triggers: none fired.

**Changed.** Host swap +2 GB; OpenCode gap 30 min. **For you.** Nothing
new; autoreport 0.1.6 and failecho-mcp 0.2.2 still unbuilt.

### 11:44 UTC (20 Sep) — pass 69

**Checked.** All active, no failed units, no tracebacks, **no OOM kill since
08:31**. 369 MB available, swap 3.1 of 6.1 GB (3.1 free). Disk 69%. One VM
and scratch file, the live run's. Production 1314, all first-party, sparse
0, independent 0.

**Found and fixed.** Two flaky tests, both test-side: the unreachable-remote
case aimed at a "free" port a later server could take (now port 9,
reliably closed), and the test HTTP server needed longer than 10 s to start
on a busy host (now 30 s, and it fails loudly instead of hanging). Full
suite after the fix: only the known concurrency test
(`test_counter_increments_are_atomic`) flapped, which passes alone.

Provider group 525 vs 520: **93.0 vs 78.1 %** marked completed, recovered
78.2 vs 20.4 %. Real 72.8 vs 66.5 %. Build 85.2 vs 84.1 %. OpenCode 76.5 vs
82.3 %. Proxy 65 vs 65 (30 min gap): 86.2 vs 84.6 %. Wrapped 86 vs 86,
100 %, advice on 40. Prod 57 vs 56: 91.2 vs 98.2 %. Onboarding 128 runs,
54 of 82. Triggers: none fired.

**Changed.** The two test fixes; report refreshed earlier (`82c2c56`).
**For you.** Nothing new.

### 12:44 UTC (20 Sep) — pass 70

**Checked.** All active, no failed units, no tracebacks, no OOM kill since
08:31. 403 MB available, swap 4.96 of 6.1 GB. Production 1328, all
first-party, sparse 0, independent 0. One VM and scratch file, the live
run's.

**Found.**
- Disk 69 to 73 % (16 GB free). Not FailEcho: the host's other projects
  (uniswapx 2.8 GB, aa 2.4 GB, arbitrage 1.6 GB, .rustup 1.5 GB, .claude
  1.3 GB, .nvm 1.1 GB). Ours: /srv 253 MB, sandbox rootfs 753 MB, lab DB
  20 MB. Left alone; worth the user knowing.
- Onboarding quiet since 10:54: gemini, ollama and openrouter all at their
  daily caps, as designed. The timer keeps firing and skipping.
- Provider group 533 vs 528: **93.1 vs 78.4 %** marked completed,
  recovered 78.2 vs 20.4 %. Real 72.7 vs 66.5 %. Build 84.7 vs 83.6 %.
  OpenCode 76.5 vs 82.2 %. Proxy 65 vs 65: 86.2 vs 84.6 %. Wrapped 89 vs
  89, 100 %, advice on 44. Prod 60 vs 59: 91.7 vs 98.3 %. Triggers: none.

**Changed.** Nothing. **For you.** Memory is tight enough that Claude Code
stopped two of my idle background shells at 12:0x; production was never
touched. If you want more headroom before the launch, the cheapest step is
pausing the OpenCode pairs (the 768 MB guests, and the least conclusive
group).

### 17:44 UTC (20 Sep) — passes 71 to 73 (one entry)

**Checked.** All active, no failed units, no tracebacks. 381 MB available,
swap 3.0 of 6.1 GB (3.1 free). Disk 76%, 15 GB free. One VM and scratch
file, the live run's. Production 1410, all first-party, sparse 0,
independent 0. Canary 04:17 green.

**Found.**
- One kernel OOM kill at 13:14, of a root `python` holding ~500 MB: my own
  test run, not a service. None since; no service has been killed.
- Disk 73 to 76 % (15 GB free), still the host's other projects, not ours.
- Provider group 573 vs 567: **93.4 vs 78.8 %** marked completed,
  recovered 78.4 vs 20.3 % — steady all day. Real 73.7 vs 67.1 %. Build
  84.7 vs 83.9 %. OpenCode 75.2 vs 83.1 % (125 vs 142 runs: the 30 min gap
  falls on whichever twin the cursor reaches, so the counts drift apart;
  worth evening out if the group is to say anything). Proxy 65 vs 65,
  unchanged, still no advice line in a scheduled run. Wrapped 103 vs 103,
  100 %, advice on 54. Prod 74 vs 73: 91.9 vs 98.6 %. Onboarding 133 runs,
  56 of 84. Triggers: none fired.

**Changed.** Nothing. **For you.** Unchanged: autoreport 0.1.6 and
failecho-mcp 0.2.2 carry the review fixes and are not built yet; the
deliverable-grading work is still a decision.

### 18:44 UTC (20 Sep) — pass 74

**Checked.** All active, no failed units, no tracebacks, no OOM kill since
13:14. 278 MB available, swap 3.1 of 6.1 GB. Disk 76%, 15 GB free. One VM
and scratch file, the live run's. Production 1418, all first-party, sparse
0, independent 0.

**Found and fixed.** The proxy pair had **not run since 10:46** while the
other OpenCode pair ran nine times: the 30 min gap consumed whichever twin
the lane cursor reached, and the cursor kept landing on the same pair. The
four OpenCode personas now take turns on a cursor of their own, so a gap
costs frequency, not fairness; test added (`6824f8d`). Deployed, lab
re-pinned with timers paused. Also relaxed the unreachable-remote test's
5 s bound to 20 s: the promise is an answer rather than a hang, and 5 s
flapped on a loaded host.

Provider group 579 vs 573: **93.4 vs 79.1 %** marked completed, recovered
78.4 vs 20.3 %. Real 73.8 vs 66.9 %. Build 85.0 vs 83.9 %. OpenCode 75.4
vs 81.5 % (126 vs 146, the imbalance above). Wrapped 107 vs 106, 100 %,
advice on 54. Prod 77 vs 76: 90.9 vs 98.7 %. Onboarding 134 runs, 56 of
84. Triggers: none fired.

**Changed.** OpenCode rotation and the test bound (`6824f8d`). **For you.**
Unchanged: 0.1.6 / 0.2.2 unbuilt; deliverable grading undecided.

### 19:44 UTC (20 Sep) — pass 75

**Checked.** All active, no failed units, no tracebacks, no OOM kill since
13:14. 517 MB available, swap 3.1 of 6.1 GB. Disk 76%. No VM or scratch
left over. Production 1429, all first-party, sparse 0, independent 0.

**Found.** The new OpenCode rotation is live and at turn 1 (its first run
under the new code was 19:33); the proxy pair comes up within the next two
OpenCode slots. Wrapped ask 109 runs, one incomplete (19:10): three
provider rate limits in a row, none of which the network had a fix for —
the agent gave up, which is the honest outcome, not a bug.
Provider group 587 vs 581: **93.4 vs 78.8 %** marked completed, recovered
78.7 vs 20.4 %. Real 73.9 vs 66.6 %. Build 85.3 vs 84.4 %. OpenCode 73.6
vs 81.5 %. Prod 80 vs 79: 91.2 vs 98.7 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Unchanged.

### 20:45 UTC (20 Sep) — pass 76

**Checked.** All active, no failed units, no tracebacks, no OOM kill since
13:14. 480 MB available, swap 3.0 of 6.1 GB. Disk 76%. No VM or scratch
left over. Production 1454, all first-party, sparse 0, independent 0.

**Found.** The VM lane is idle for the rest of the UTC day, by cap: NVIDIA
2404 of 2400 calls, xKiro 953k of 950k tokens, groq out of quota since
19:17. So both OpenCode pairs and the builders wait for 00:00; the
rotation cursor is still at 1 and the proxy pair's turn comes after the
reset. Light lane and onboarding unaffected.
Provider group 586 vs 579: **93.5 vs 78.9 %** marked completed, recovered
79.2 vs 20.5 %. Real 73.8 vs 66.3 %. Build 85.2 vs 84.3 %. Wrapped 113 vs
114, 99.1 vs 100 %. Prod 80 vs 79: 91.2 vs 98.7 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Unchanged: 0.1.6 / 0.2.2 unbuilt,
deliverable grading undecided; the pre-launch check runs at 06:43 UTC.

### 08:2x UTC (22 Sep) — pre-launch check (scheduled; it fired a day late)

**Checked.** Every unit and timer active, none failed; canary 04:16 green;
no OOM kill in six hours; disk 77%, 14 GB free. Production 1986
observations, all first-party, sparse 0, independent 0, demo_data false.
Pages: /, /setup, /about, /docs, /llms.txt, /network, /demo and /mcp all
200. The llms.txt verify line runs and answers INSUFFICIENT_DATA /
known:false, as the page says it should. Clean installs from the
registries (env passed explicitly to the lab): failecho-autoreport 0.1.5
gave advice on a real 503 ("try retry, worked 209/306"); failecho-mcp
0.2.1 worked in relay and proxy modes from both pip and npx. Production
untouched by the checks.

**Found and fixed.** The setup page promised the proxy "never ... the error
text" and "byte for byte" for every message. Neither holds for the
published 0.2.1: it honours FAILECHO_SEND_ERRORS, and a remote server's
messages are parsed and re-serialised. The page now says exactly that and
names 0.2.2 as the version that ignores the switch (`4765a30`, live).

**Built, not published.** autoreport 0.1.6 and failecho-mcp 0.2.2 carry the
review fixes; twine check passes, npm tarball packed.

**For you.** Go / no-go is in the reply: the site and the service are
ready; the published packages still carry two review defects.

### 08:44 UTC (22 Sep) — pass 78

**Checked.** All active, no failed units, no tracebacks, no OOM kill. 536
MB available, swap 3.0 of 6.1 GB, disk 77%. One VM and scratch file, the
live run's. Production 1991, all first-party, sparse 0, independent 0.

**Found.** The user published autoreport **0.1.6** and failecho-mcp
**0.2.2** (PyPI and npm). Verified from the registries in clean installs:
both modules byte-identical to source, `503 invalid upstream response` now
classifies as server_error/503, the advice line carries its caveats
("evidence score", decay, scope), relay and proxy modes work from pip and
npx. Production untouched.
Provider group 529 vs 526: **93.8 vs 83.1 %** marked completed, recovered
77.8 vs 16.8 %. Real 68.3 vs 63.4 %. Build 84.0 vs 83.9 %. OpenCode 69.8
vs 78.1 %. Proxy pair 27 vs 26 (rotation working again): 74.1 vs 92.3 %,
still no advice line. Wrapped 158 vs 158, 99.4 vs 100 %, advice on 87.
Prod 118 vs 118: 91.5 vs 94.1 %. Onboarding 190 runs, 81 of 122.
Triggers: none fired.

**Changed.** The setup page's proxy promise is unconditional again, now
that 0.2.2 ignores `FAILECHO_SEND_ERRORS`; deployed. **For you.** Nothing
outstanding on the packages.

### 09:44 UTC (22 Sep) — pass 79

**Checked.** All active, no failed units, no tracebacks, no OOM kill. **724
MB available, swap 1.5 of 6.1 GB** after the user let me stop the host's
other projects (uniswapx x3, mev, across-scan): about 80% of a core freed
too. Disk 77%. One VM and scratch file, the live run's. Production 2000,
all first-party, sparse 0, independent 0.

**Found and fixed.** A proxied run (09:43) made six `pypi_latest` calls and
the lab got **two** reports: NVIDIA was answering 503 "service
overloaded", the run ended on the 280 s clock, OpenCode killed its MCP
servers, and the guest was destroyed before the proxy flushed its queue.
Three seconds at the end of the guest command fixes it (`d09d8c9`),
checked with `sh -n` and a dry run; deployed, lab re-pinned with timers
paused. This is the data the new flaky tools exist to collect, so it
mattered.

Provider group 532 vs 529: **94.0 vs 83.6 %** marked completed, recovered
77.3 vs 17.2 %. Real 68.8 vs 64.5 %. Build 83.9 vs 83.7 %. OpenCode 69.8
vs 76.9 %. Proxy pair 28 vs 26: 71.4 vs 92.3 %, no advice line yet (the
flaky tools went live at 09:1x; the first tasks using them have not run).
Wrapped 157 vs 157, advice on 87. Prod 121 vs 121: 91.7 vs 94.2 %.
Triggers: none fired.

**Changed.** Guest flush pause (`d09d8c9`); earlier this hour the flaky
tools and the 10 min gap (`f28b907`). **For you.** Nothing.

### 10:44 UTC (22 Sep) — pass 80

**Checked.** All active, no failed units, no tracebacks, no host OOM. 790
MB available, swap 1.5 of 6.1 GB. Disk 77%. No VM or scratch left over.
Production 2022, all first-party, sparse 0, independent 0.

**Found and fixed.** The new flaky tool is being used (service_status: 5
calls in a blind run, 1 in a proxied one) but the lab was still missing
most reports: the 10:30 proxied run made 11 tool calls and **one** report
arrived. Cause: **the guest kernel killed it** (exit 137, oom 1). The
proxied twin runs OpenCode *and* the proxy *and* the packages server in
one 768 MB guest, and it no longer fits; the queued reports died with the
process. Guest memory to **1024 MB**, VM-lane MemoryMax 1100M to 1400M
(`8e6da31`); the host has the room now that its other projects are
stopped. Deployed, unit installed, lab re-pinned with timers paused.

Provider group 527 vs 523: **93.7 vs 83.6 %** marked completed, recovered
77.6 vs 16.5 %. Real 68.6 vs 64.4 %. Build 84.2 vs 83.8 %. OpenCode 68.0
vs 77.4 %. Proxy pair 25 vs 24: 72.0 vs 91.7 %, still no advice line.
Wrapped 156 vs 156, advice on 86. Prod 121 vs 120: 91.7 vs 94.2 %.
Onboarding 193 runs, 81 of 123. Triggers: none fired.

**Changed.** Guest memory (`8e6da31`). **For you.** Nothing.

### 11:44 UTC (22 Sep) — pass 81

**Checked.** All active, no failed units, no tracebacks, no OOM kill since
10:30. 614 MB available, swap 1.5 of 6.1 GB. Disk 77%. One VM and scratch
file, the live run's. Production 2041, all first-party, sparse 0,
independent 0.

**Found.**
- **Grading is live and working**: 3 runs graded so far, e.g. 11:24
  ocp-blind `{answered: true, valid: true, correct: true, checked: 3,
  matched: 3}`. The flaky-tool tasks have no external truth to check, so
  they grade as `checked: 0` — correct, not a gap.
- The new rows are in the scoreboard: "result complete and not a
  placeholder" and "every checked value correct", with the count of runs
  where truth was checkable beside them.
- The local-recovery arm has **0 runs** so far; the light-lane cursor has
  not reached it (47 personas, one a minute). First numbers within the
  hour.
- Proxy pair 22 vs 22: 68.2 vs 90.9 %, still no advice line; its flaky
  tasks have run but the network has no evidence for `packages` yet.
- Provider group 519 vs 517: **93.4 vs 83.8 %** marked completed, recovered
  76.8 vs 17.0 %. Real 68.3 vs 64.7 %. Build 84.7 vs 83.8 %. OpenCode 67.3
  vs 76.7 %. Wrapped 156 vs 156, advice on 86. Prod 121 vs 120: 91.7 vs
  93.3 %. Triggers: none fired.

**Changed.** Nothing this pass (fixes 2 and 3 went in at 11:0x and 11:3x:
`2b5bd9b`, `197936c`). **For you.** Two memory decisions are open: whether
I may set systemd OOM priorities so production can never be the victim
(free, ten minutes), and whether a bigger host is acceptable.

### 13:42 UTC (22 Sep) — pass 82

**Checked.** All active, no failed units, no tracebacks, **no OOM kill at
all** since the priorities went in. 608 MB available, swap 1.5 of 6.1 GB.
Disk 77%. One VM and scratch file, the live run's. Production 2068, all
first-party, sparse 0, independent 0.

**Found and fixed.** The proxy pair has read "tool errors carrying advice:
0.0" since it was built, and **the counter was wrong, not the proxy**.
Driven by hand against a live GitHub 403 through the published proxy, it
attached: *"FailEcho (evidence from api.github.com): no clear fix yet; on
this service's other operations, agents tried wait_until_reset worked 3/15,
backoff worked 372/8225, ..."*. The Events parser matched `FailEcho: `
only, so the upstream-fallback shape never counted. Both shapes count now,
with a test for each (`38f0052`), deployed, lab re-pinned.

**Worth watching.** The lab holds 22 rate-limit failures for the `packages`
server and **zero recovery outcomes** for it: an agent that gives up after
a 403 never produces one, so that service can never get a recommendation
of its own. The upstream fallback is what makes the advice useful at all
there — which is the naming gap from pass 51, now doing its job.

Local arm 6 vs 6 runs (second pair added at 12:5x), 100 vs 100 % so far.
Grading: 7 runs graded, 4 checkable. Provider group 505 vs 503: **93.7 vs
84.7 %**, recovered 75.9 vs 16.5 %. Real 68.2 vs 64.9 %. Build 84.2 vs
82.9 %. OpenCode 65.2 vs 76.4 %. Wrapped 157 vs 157, advice on 84. Prod
121 vs 121: 92.6 vs 93.4 %. Triggers: none fired.

**Changed.** The advice counter (`38f0052`). **For you.** The proxy *is*
advising; the next passes will show how often, now that it is counted.

### 14:42 UTC (22 Sep) — pass 83

**Checked.** All active, no failed units, no tracebacks, no OOM kill. 633
MB available, swap 1.5 of 6.1 GB. Disk 77%. One VM and scratch file, the
live run's. Production 2080, all first-party, sparse 0, independent 0.

**Found.** No advice line in a scheduled run yet, and it is only timing:
the two proxied runs since the counter fix drew the npm/crates and PyPI
tasks, which succeeded. The failures at 13:43 in the lab were my own hand
test, not a run. The flaky tools do report: `quota_check` has 2 rate-limit
failures recorded, and its upstream is httpbingo.org, where the network
holds real evidence — so the next proxied run that draws a GitHub or
quota task should carry a line.
Local arm 12 vs 12 runs (24 total since the second pair), 100 vs 100 %.
Grading: 5 checkable runs. Provider group 500 vs 497: **94.2 vs 85.7 %**,
recovered 76.2 vs 17.8 %. Real 68.2 vs 65.2 %. Build 83.9 vs 82.7 %.
OpenCode 63.6 vs 75.9 %. Wrapped 158 vs 158, advice on 84. Prod 122 vs
122: 93.4 vs 93.4 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 15:42 UTC (22 Sep) — pass 84

**Checked.** All active, no failed units, no tracebacks, no OOM kill. 320
MB available with a guest up, swap 1.9 of 6.1 GB. Disk 77%. One VM and
scratch file, the live run's. Production 2098, all first-party, sparse 0,
independent 0.

**Found and fixed.** The local arm was measuring nothing. Its careful
rules applied only to **provider** failures, and Mistral failed **0 times
in its first 32 runs**: every failure it actually met was a GitHub 403 at
the tool level, where both twins behaved exactly like every other pair.
Two fixes (`7d02d45`): the careful plan now applies to tool failures too
(minus `switch_model`, meaningless there), and `local_plan` did not know
**`x-ratelimit-reset`** — GitHub's own header, and GitHub is where most of
the fleet's rate limits come from — so it backed off blindly precisely
where a competent client waits for the stated reset. A test pins the
difference: on a 403 carrying a reset, the local twin waits for it, the
ordinary control sleeps three seconds.

Still no advice line in a scheduled proxy run: the last three proxied
draws were pypi/npm/crates tasks, which succeed. Local arm 16 vs 16.
Grading: 5 checkable. Provider group 494 vs 490: **94.3 vs 85.7 %**,
recovered 77.2 vs 18.3 %. Real 68.1 vs 65.3 %. Build 83.7 vs 83.1 %.
OpenCode 61.0 vs 75.0 %. Wrapped 158 vs 157, advice on 84. Prod 122 vs
122: 93.4 vs 93.4 %. Triggers: none fired.

**Changed.** The local arm's rules and the reset header (`7d02d45`).
**For you.** Nothing.

### 16:42 UTC (22 Sep) — pass 85

**Checked.** All active, no failed units, no tracebacks, no OOM kill. 603
MB available, swap 1.5 of 6.1 GB. Disk 77%. One VM and scratch file, the
live run's. Production 2110, all first-party, sparse 0, independent 0.

**Found.** The local arm passed **20 runs a side** (22 vs 21) and the new
rules are visibly at work: one GitHub 403 carrying a reset header was
recovered by waiting for it (`local_plan: ['wait_until_reset']`,
recovered), which is the behaviour that did not exist before pass 84.
Early split, far too small to mean anything yet:

| | with FailEcho | local only |
|---|---|---|
| runs | 22 | 21 |
| failures met | 7 | 9 |
| recovered | 1 (14 %) | 0 (0 %) |
| attempts per failure | 1.71 | 2.0 |

Both sides still 100 % on runs marked completed. Most of the local arm's
failures remain GitHub 403s without a token, which nothing fixes.
Still no advice line in a scheduled proxy run (the draws keep landing on
tasks that succeed). Grading: 6 checkable. Provider group 488 vs 484:
**94.3 vs 85.1 %**, recovered 78.0 vs 17.8 %. Real 67.8 vs 64.7 %. Build
83.9 vs 83.9 %. OpenCode 64.1 vs 76.5 %. Wrapped advice on 86. Prod 122 vs
122: 93.4 vs 92.6 %. Triggers: none fired.

**Changed.** Nothing. **For you.** The local arm has numbers now; they are
too small to read, and the honest read is "wait for ~50 a side".

### 17:42 UTC (22 Sep) — pass 86

**Checked.** All active, no failed units, no tracebacks, no OOM kill. 648
MB available, swap 1.5 of 6.1 GB. Disk 77%. No VM or scratch left over.
Production 2135, all first-party, sparse 0, independent 0.

**Found.** Quiet hour. The 16:46 proxied run drew the GitHub release task
and GitHub answered it, so no failure and no advice line; the 17:17 run was
the direct twin. Advice lines in scheduled runs: still 0, purely which task
comes up — the reliable failure in that pair is `quota_check`, one draw in
six. Local arm 26 vs 26, both 100 %. Grading: 7 checkable runs. Provider
group 481 vs 479: **94.4 vs 85.6 %**, recovered 77.9 vs 19.7 %. Real 67.2
vs 64.4 %. Build 83.6 vs 83.1 %. OpenCode 63.2 vs 77.6 %. Wrapped advice
on 86. Prod 122 vs 122: 93.4 vs 92.6 %. Triggers: none fired.

**Changed.** Nothing. **For you.** Nothing.

### 18:49 UTC (22 Sep) — pass 87

**Checked.** All active, no failed units, no tracebacks, no OOM kill. 671
MB available, swap 1.4 of 6.1 GB. Disk 77%. No VM or scratch left over.
Production 2147, all first-party, sparse 0, independent 0.

**Found.** The stricter MCP rules went live at 18:48 (`8a760b6`); the MCP
twin's last run was 15:44, so the "since the rules" rows are still empty.
First reading expected overnight — that twin comes up roughly every two
hours. No advice line in a scheduled proxy run yet either; the last two
proxied draws were npm/crates and the GitHub release task, which GitHub
answered. Local arm 34 vs 34, both 100 %. Grading: 2 vs 2 checkable in the
proxy pair. Provider group 477 vs 475: **94.3 vs 85.5 %**, recovered 77.9
vs 19.7 %. Real 66.8 vs 63.9 %. Build 83.1 vs 82.7 %. OpenCode 63.2 vs
78.3 % (all-time, i.e. the polite-paragraph era). Wrapped advice on 86.
Prod 120 vs 120: 93.3 vs 92.5 %. Triggers: none fired.

**Changed.** Nothing this pass. **For you.** Nothing.

### 20:05 UTC (22 Sep) — three builds, deployed

Not a watch pass: the three items the user picked off the "what is left"
list, built in order.

**Browser adapter** (`3f4cf5d`). `failecho_commerce/browser.py` wraps a page
object the way the wrapper wraps a function. No Playwright import, so it is
testable without a browser this host cannot afford. Catches a 403 challenge
where it happens rather than one click later, counts a declined card as
nothing at all, and lets the page outvote the driver's words. 17 tests.

**Measured numbers on /setup and llms.txt** (`bed7c92`). 0.17 FailEcho calls
per run, 1.2 retry attempts per failure against 1.99, 1.12 s against 2.31 s
lost inside failures, 1,737 tokens against 1,738 — from the production-reading
arm, 118 runs a side. A test enforces that both qualifiers travel with them:
our own agents, and a control that retries once after three seconds.

**Signed reporter identity** (`65e19cf`). A reporter id can be an Ed25519
public key with each request signed; a bad signature is a 400, never a silent
downgrade. Ed25519 in pure Python on both sides (the client has no
dependencies; the server prefers `cryptography` when present), checked against
RFC 8032 and against each other. `verified_reporters` in `/v1/stats`. Stated
everywhere it appears: keys are free, so this is not Sybil resistance.

**Private mode, free for early teams** (`5706a41`). `X-FailEcho-Team: <secret>`
stores a report for one team in its own tables — isolation by table, not by
flag, so no aggregation path can leak it. A query gets the public answer plus
`team_evidence`, held to the same 5-attempt, 60% bar. No account: the token is
self-chosen and stored as a salted hash. Kept 30 days. Pricing written down in
`docs/business-model.md`: free while bootstrapping, `FIN_PRIVATE_MODE_OPEN` is
the switch for when that stops being true.

**Deployed** 8a760b6 → 5706a41 after a backup
(`backups/pre-private-mode-20260922T200302Z.db`). All pages 200,
`verified_reporters` 0, `observations_total` 2,174, independent still 0. Lab
stays pinned at 42ffc2c, so the experiment under the fleet did not move.

### 20:20 UTC (22 Sep) — pass 88

**Checked.** All units active, none failed, no host OOM kill, no stray VM or
scratch image. 653 MB available, swap 1.4 of 6.1 GB, disk 77%. Production
2,174 observations, all first-party, independent 0, sparse 0, verified 0.

**Found.** Two things. Production has been printing a full traceback roughly
hourly on `/mcp` — `ClientDisconnect`, a peer hanging up mid-request, 56 since
21 Sep; harmless, and exactly the noise that hides a real one. Fixed.

Second, the VM lane has been idle since 17:16: nvidia is at its daily cap
(2,403 of 2,400), xkiro at its cap, groq's quota gone since 18:17. Every
OpenCode persona rides one of those, so the stricter-MCP-rules experiment has
had **no runs at all** since the change went in at 18:48 and will get none
until the UTC day rolls. The caps are working as designed; the MCP twins are
simply last in the queue behind a light lane that spends the same budget.

**Local arm passed the mark you asked about**: 42 runs a side. Both sides
complete 100% — the local recovery logic is good enough that the network
cannot add completion — but the asking side is faster: **7.3 s vs 9.7 s per
run**, 5.35 s vs 7.84 s lost inside failures, 1.85 vs 2.0 attempts per
failure. Against a competent engineer the gain is time, not outcomes.

Proxy: still **0.0 tool errors carrying advice per run** on both sides, 18 vs
19 runs. Grading: 2 vs 2 checkable in the proxy pair, 2 vs 1 in the MCP pair —
far from 20. Groups unchanged in direction: provider 94.3 vs 85.4 (recovered
77.9 vs 19.7), real 65.8 vs 63.0, build 83.0 vs 82.6, test 20.4 s vs 27.7 s,
prod 93.2 vs 92.4, wrapped 99.4 vs 100, opencode 60.0 vs 77.8 (all-time).
Lab recovery evidence: backoff 1,572/11,661, retry 1,103/2,693, switch_model
291/329. Triggers: none fired (no guest OOM today, ask twin does call the
tools at 0.17/run).

**Changed.** `9a8d12a` — catch `ClientDisconnect` on the MCP endpoint, with a
test. Deployed; all pages 200.

**For you.** The MCP hard-rule experiment is starved, not broken: the light
lane spends nvidia's daily budget before the VM lane gets a turn, so the
"since the rules" rows stay empty for another four hours every day. One line
would fix it — reserve a slice of each provider's daily cap for the OpenCode
twins, so the experiment you asked for gets its runs first and the light lane
stops slightly earlier. Both arms lose the same runs, so no comparison moves.
Say the word and it goes in next pass; caps themselves stay where they are.

### 20:50 UTC (22 Sep) — grading coverage, and the reserve live

**Changed.** `6343007` — the OpenCode reserve: everything that is not an
OpenCode persona now stops at 85% of a provider's daily cap, so the last 15%
is spendable only by the twins. Caps unchanged, both arms of every pair lose
the same runs. Visible in the journal as `skip ... (OpenCode reserve)`. It
bites from the 00:00 UTC reset; nvidia was already past its full cap today.

`5100f4a` — grading coverage. The grader read JSON result files only, so the
light lane's 963 runs a side had no grade at all and the OpenCode pair had 2
checkable of 35. Now: prose answers are graded line by line against truth
fetched from the same APIs the task names (a version must appear as itself —
0.12.171 is not 0.12.17 — while a sentence-ending full stop does not break the
match); three tasks are graded with no service to ask, from arithmetic and
from what the tool can possibly have returned (`quota_check` answers 429 every
time, so a number is an invention and a reason naming the refusal is correct);
and the two tasks whose right answer is a denial get `pypi_absent` /
`github_absent`, where a 404 is the answer rather than a failure to get one.
Every task the fleet runs has a grader, with a test that it stays that way,
and the three grading rows now appear for every group.

**Checked.** Suite green. Lab re-pinned to `5100f4a`, both timers restarted,
first graded light-lane run landed at 20:47:33: npm react/vue/svelte, valid,
correct, 3 of 3 checked. Abstention unchanged — no truth, no grade.

**For you.** Nothing to decide. The real-group correctness rate needs a few
hours of runs before it means anything; expect a first reading next pass.

### 21:05 UTC (22 Sep) — pass 89

**Checked.** All units active, none failed, no host OOM kill, no stray VM or
scratch image, no traceback since the `ClientDisconnect` fix went in. 732 MB
available, swap 1.45 of 6.1 GB, disk 77%. Production 2,177 observations, all
first-party, independent 0, sparse 0, verified 0.

**Found.** The widened grader is working and honest: since 20:45 the fleet
recorded 4 runs, 2 of them from gradable tasks, and both were graded — valid,
correct, 3 of 3 values each. The other two were an explorer and a limits
persona, which have no external answer to check, and correctly got no grade.
Across every graded record: **23 values checked, 2 abstained (8%)** — the
grader is grading, not abstaining. Grading rows now show on the real group
(1 vs 1 checkable so far) and will fill in for prod and local as runs land;
prod rides nvidia and is capped until midnight, local rides mistral and is
running.

The rate is low because almost everything is capped: 43 fleet ticks since
20:45 produced 4 runs. The OpenCode reserve is visibly holding (`skip
fleet-decor-ask-c: nvidia at its daily cap (OpenCode reserve)`), and no MCP
twin has run since 17:16, so "since the stricter rules" is still 0 vs 0.

Local arm now **46 vs 46**, both 100% completed, **6.8 s vs 9.1 s** per run,
4.88 vs 7.16 s lost inside failures, 1.86 vs 2.0 attempts per failure —
direction unchanged from pass 88. Proxy still 0.0 advice lines per run, 18 vs
19 runs, no new runs. Standing groups unchanged in direction: provider 93.8 vs
84.9 (recovered 75.9 vs 19.2), real 65.5 vs 62.6, build 83.0 vs 82.8, test
20.3 s vs 27.6 s, prod 93.1 vs 92.2, wrapped 99.4 vs 100, opencode 63.6 vs
77.3 all-time. Advice table: 252 skips on Stack Exchange 429s where blind
retried 248 times and recovered **0**. Canary 17 of 17 steps green at 04:16;
onboard 200 runs, 84 of 126 graded passes. Triggers: none fired.

**Changed.** Nothing this pass.

**For you.** Nothing to decide. Both things you are waiting on are pinned to
the UTC reset: the MCP twins get their reserved budget at 00:00, and the
grading rows for prod and local fill in with them.

### 21:58 UTC (22 Sep) — pass 90

**Checked.** All units active, none failed, no host OOM kill, no stray VM or
scratch image, and no traceback of any kind since the 499 fix — the
`No response returned` it replaced has not come back either. 687 MB available,
swap 1.38 of 6.1 GB, disk 77%. Production 2,191 observations, all first-party,
independent 0, sparse 0.

**Found — a grader misgrade, and it was ours.** The first graded hours said
the local arm got **0% of its values right while completing 100% of its
runs**. That was the grader, not the models: a model answers "**LlamaIndex:**
836 open issues", which is the right number, and the grader looked for the
line mentioning `llama_index`, found none, and scored the run wrong. Same for
`python-sdk` against "Python SDK". Punctuation is ignored on both sides now,
and a name still has to appear — "the other one has 836" is still incomplete.

Grading reached **20 checkable runs on the ask side** (14 blind) before the
fix, which is the mark you asked to hear about; those numbers are now void,
see below. The grader was abstaining on 10% of values, so it is grading, not
guessing.

Local arm **53 vs 52** runs, both 100% completed. Proxy: 0.0 advice lines per
run, no new runs. MCP twins: still 0 vs 0 since the stricter rules, nvidia
capped until 00:00. Groups unchanged in direction: provider 93.8 vs 84.8,
real 65.1 vs 62.3, build 82.8 vs 82.6, prod 93.0 vs 92.1, wrapped 99.4 vs 100,
opencode 65.6 vs 78.6. Lab recovery evidence: backoff 1,583/11,814, retry
1,106/2,716, switch_model 293/332. Canary and onboard unchanged. Triggers:
none fired.

**Changed.** `20ed48a` — match the name a model actually writes; record which
value missed (the label, never what it said); keep 200 characters of an answer
the grader called wrong, and only those, so the next dispute can be settled by
looking. `9a8c077` — `GRADING_SINCE`: grades recorded by the broken grader are
not counted, the way `MCP_RULES_SINCE` already splits the MCP twin. About
seventy runs lost; a rate that averages a bug with its fix is worth less.
Lab re-pinned, timers restarted.

**For you.** The correctness rates restart from now. Expect the first
trustworthy reading next pass — and it will be lower than the completion rate,
which is the entire reason the grading work happened.

### 22:50 UTC (22 Sep) — pass 91

**Checked.** All units active, none failed, no host OOM kill, no stray VM or
scratch image, no traceback since the 499 fix. 578 MB available, swap 1.4 of
6.1 GB, disk 78%. Production 2,201 observations, all first-party, independent
0, sparse 0.

**Found — the grader was punishing honesty.** The first grades from the fixed
grader came back with the missed labels named, and four of the five misses
were the same thing: "Could not fetch open issues due to rate limits", "Data
unavailable", "I can't retrieve the current star counts right now due to a
GitHub rate-limit error". GitHub had genuinely rate-limited the lab. The
agents declined to invent a number — the behaviour this whole network argues
for — and the grader scored it exactly like a fabrication. That is now a third
outcome: a declined value leaves the correctness rate instead of failing it,
and every group carries "values declined rather than invented". A denial is
still an answer: the task asking whether a package exists wants "it does not".

This was findable only because the previous pass started naming the missed
label and keeping the head of a disputed answer. That is what those two lines
were for.

**Grading, from the fixed grader:** local **83.3% vs 66.7%** correct on 6 vs 6
checkable, real 75.0 vs 100.0 on 4 vs 3, wrapped 100 vs 100 on 3 vs 2. Small
numbers, honest ones. The grader abstained on 5% of values.

Local arm **60 vs 60** runs, both 100% completed, 9.9 s vs 13.9 s per run.
Proxy still 0.0 advice lines, no new runs. MCP twins 0 vs 0 since the rules;
nvidia is capped until the 00:00 reset, when the reserve takes effect.
Standing groups unchanged in direction: provider 93.5 vs 84.7 (recovered 75.0
vs 19.2), real 64.6 vs 62.4, build 82.8 vs 82.5, test 20.0 s vs 27.2 s, prod
92.9 vs 92.0, opencode 65.6 vs 76.3. Canary green, onboard 201 runs. Triggers:
none fired.

**Changed.** `9c8bad9` — count a declined value apart from a wrong one, with
the scoreboard row and tests. Lab re-pinned, timers restarted.

**For you.** The first trustworthy correctness numbers are in and they are
below the completion rates, as expected: local completes 100% of runs and gets
83% of its values right. Nothing to decide.

### 23:45 UTC (22 Sep) — pass 92

**Checked.** All units active, none failed, no host OOM kill, no stray VM or
scratch image, no traceback since the 499 fix. 645 MB available, swap 1.41 of
6.1 GB, disk 78%. Production 2,221 observations, all first-party, independent
0, sparse 0.

**Found.** Nothing broken. The whole VM lane and most of the light lane are
waiting for the UTC reset, and every skip has a reason that checks out:
nvidia 2,403 calls of 2,400, xkiro **968,234 tokens of 950,000** (a token cap,
not a call cap), groq marked dead at 22:45 when its quota went — that is the
same refusal that produced the `(provider failed: rate_limit)` record the
grader used to score as three wrong answers. Only mistral has budget left
(865 of 2,400), so the light lane is running on one provider and the MCP twins
have not moved since 17:16. Eighteen minutes to the reset, when the reserve
gives them the last 360 nvidia calls.

Grading, from the fixed grader: local **91.7% vs 75.0%** correct on 12 vs 12
checkable, real 100 vs 100 on 6 vs 6, wrapped 100 vs 100 on 6 vs 5. No values
declined this hour, and no abstentions worth reporting. Local arm 66 vs 66
runs, both still 100% completed. Proxy 0.0 advice lines, no new runs.
Standing groups unchanged in direction: provider 93.5 vs 84.8, real 64.4 vs
61.9, build 83.1 vs 82.7, prod 92.7 vs 91.8, wrapped 99.4 vs 100, opencode
65.6 vs 77.8. Triggers: none fired.

**Changed.** Nothing this pass.

**For you.** Nothing to decide. The local arm's correctness gap has held
direction for three readings now (83/67, 90/70, 92/75) on equal samples — the
first sign that asking the network changes what an agent gets *right*, not
just how fast it fails. Still small; 12 runs a side.

### 03:40 UTC (23 Sep) — pass 93

**Checked.** All units active, none failed, no host OOM kill, one live VM-lane
run and its scratch image (expected), no traceback since the 499 fix. 495 MB
available, swap 1.44 of 6.1 GB, disk 78%. Production 2,260 observations, all
first-party, independent 0, sparse 0.

**Found — two more grader misgrades, both ours.** 42 runs had been graded
wrong since the grader started working, and the disputed answers kept last
pass say why. A value often arrives *below* the summary line — "**npm
express** is newer." with the numbers in the list under it — and the grader
took the first line naming a package and stopped. And zero is an answer: a
relative tolerance around it is undefined, so `_within` refused it and "0 open
issues", the true count for our own repository, was wrong every time it was
right. That was 21 of the 42. A release tag written `v0.1.0` or `0.1.0` now
counts either way; `v0.1.01` still does not.

**Grading passed 20 checkable runs a side** — real 35 vs 32, local 25 vs 25,
wrapped 11 vs 11 — which is the mark you asked to hear about. Read them after
the fix lands, not before: the rates below still include the misgraded runs.
Real 80.0 vs 78.1, local 80.0 vs 80.0, wrapped 90.9 vs 81.8, prod 66.7 vs
83.3. **The local arm's direction changed**: 91.7/75.0 last pass, tied now.

MCP twins ran four times since the stricter rules (2 a side) and called
FailEcho **zero** times — but met zero failures too, all four using `bash` and
`webfetch`, so the rules were never in play. Trigger 9 needs six runs that met
a failure; we have none. Proxy 20 vs 20 runs, still 0.0 advice lines. Standing
groups: provider 92.7 vs 84.3 (recovered 71.8 vs 21.5), real 64.6 vs 62.3,
build 83.0 vs 82.1, prod 93.3 vs 91.3, wrapped 99.4 vs 100, opencode 69.2 vs
65.4. Advice table: 281 skips on Stack Exchange 429s where blind retried 278
times and recovered 0. Lab evidence: backoff 1,624/12,312, retry 1,151/2,813,
switch_model 301/342. Canary 17 of 17, onboard 213 runs. Triggers: none fired.

**Changed.** `462ef42` — the OpenCode plugin (`failecho-opencode`) and the
fourth lab arm, `fleet-och-ask-n` against `fleet-och-blind-n`: OpenCode with
the plugin against plain OpenCode, on the MCP pair's own tasks. The plugin
hooks `tool.execute.after` for every tool OpenCode runs, so nothing is left
for the model to choose. `552c6ad` — the two misgrades above. Lab re-pinned
twice, timers restarted.

**For you.** The hook arm is live and will produce its first runs on the
reserve tonight. That is the comparison that settles whether the product helps
a real agent product, because it is the only OpenCode arm where the model is
never asked to do anything.

### 05:20 UTC (23 Sep) — audit of the numbers and the suite

**Checked.** Every scoreboard figure against the raw ledger. No ordering bias
between twins on the shared GitHub quota (0.873 vs 0.850 rate-limit failures
per run across all pairs). The skip verdict holds: where askers were told to
skip, blind retries of the same shape recovered 0/272 on Stack Exchange, 0/161
on httpbingo, 22/446 on GitHub. In the competent-control local arm, asking
the network did **not** improve recovery on GitHub 403s (4/29 vs 12/42, not
significant) — it reduced failures met and time lost. The headline provider
result is against a naive retry and must keep saying so.

**Found.** The ledger keeps 5000 runs, about 2.5 days, so the slow arms could
never grow (OpenCode MCP twin 35/45 → 23/22 while running daily), and the
page said "since 17 Sep" over tables holding 2.5 days. Grades were frozen at
record time, so each grader fix voided every grade before it — and five more
misgrades were in the rejected answers (digit groups with spaces, a
one-subject answer without the subject's name, the harness's "(model budget
exhausted)", "cannot be found", a value-less line under a refusal). A flaky
test was the clock crossing an hour against a per-hour cap.

**Changed.** `af58645`: an archive keeps the slow arms' runs 30 days; the
report and page state each table's window; grades keep the truth they used
and are recomputed by the current grader at report time; GRADING_SINCE moves
to 03:32; the five misgrades fixed; API tests start clear of the hour; two
real sleeps and a per-test uvicorn start removed; `pytest -n 2` documented —
1m22s instead of 2m30s. Deployed, lab re-pinned, window live on the page.

**For you.** Correctness counts restart from 03:32, now on a grader whose
future fixes apply backwards. Expect 20 checkable a side again by this
evening.

### 06:20 UTC (23 Sep) — the team arm is live

**Changed.** `ecb9926`: four personas measure private mode the way a small
team would use it — two askers sharing one team token and reading only their
team's own evidence, two controls reading nothing, both recovering carefully,
on the real crates.io / Stack Exchange / GitHub-search limits job, no model.
New rows: hours until the team's own history had its first fix, and the share
of the team's failures that had one. Lab re-pinned.

**Checked.** One real run each, started by hand so as not to wait for the
rotation: the asker made 21 tool calls and met 6 failures, the control 20
and 5. In the lab database: 21 private observations under one team, 12
failures, 6 private recovery outcomes, and 0 rows from that agent in the
public tables. The team token was created 0600 in the fleet's state directory.

**For you.** One gap the first run already shows: the public network's most
valuable verdict — "skip, nothing has worked" (Stack Exchange: blind retried
278 times, recovered 0) — has no private equivalent. `team_evidence` only
recommends an action that worked, so a team that keeps hitting an exhausted
quota will never be told to stop. All 8 of the team's Stack Exchange calls
failed on its first run.
