# Publish checklist, 21 September 2026

Green-lit by the user on 19 September. What is done, what is left, and who
does each step.

## Done (19 Sep)

- `failecho-autoreport` 0.1.4 built in `dist/`, `twine check` passes,
  installed in a clean venv and tried: advice read from the lab, a dead
  endpoint costs nothing. Adds opt-in advice: on a failure the wrapper asks
  the network and attaches the answer to the exception (`08f063a`).
- The package README says what advice does and that the network has no
  independent reporters yet (`8320e09`).
- The setup page's advice section is held on branch `docs-advise` so the
  site does not advertise a version PyPI does not have (`0040583`).
- The lab runs the shipped path: `fleet-wrap-ask` / `fleet-wrap-blind`,
  tools wrapped by the package, no harness help (`817a8ce`).

## Left

1. **Done 19 Sep 05:0x (user):** published; verified from PyPI in a clean venv, identical to source.
   `cd /root/agentwebsite && .venv/bin/python -m twine upload dist/failecho_autoreport-0.1.4*`
2. **Done:** `docs-advise` merged and deployed (`b6d6dba`); live on /setup. Was: merge `docs-advise`, deploy, check
   the page.
3. **Claude, 20 Sep:** refresh `docs/report-48h.md` with the wrapped pair's
   first numbers and the honest summary: FailEcho raises completion where
   a fix exists (provider limits), saves retries where none does (GitHub
   403 without a token), and cannot help with an agent's own bugs.
4. **Claude, 21 Sep morning:** full pass: all units, canary, `/v1/stats`
   independent 0 and shown as 0, llms.txt verify line, front page loads,
   `pip install failecho-autoreport` from PyPI in a clean venv.
5. **User:** write the intro and post.

## failecho-mcp 0.2.0 (the proxy)

Built and checked 19 Sep (`bec5b14`): both packages installed clean, relay
and proxy modes work against real servers; 50 proxy tests across both
languages. Setup-page section held on `docs-proxy` (`4248736`).

1. **Done 19 Sep 06:2x (user); verified from PyPI, proxy.py identical to source, relay and proxy modes work.** PyPI: `cd /root/agentwebsite && TWINE_USERNAME=__token__
   TWINE_PASSWORD='pypi-...' .venv/bin/python -m twine upload dist/failecho_mcp-0.2.0*`
2. **Done (user; npm latest 0.2.0 by 06:31, verified via npx from a clean cache, proxy.js identical to source):** npm: `npm login` once, then `./scripts/publish_npm_relay.sh`. Then Claude adds the npx line to /setup.
3. **User, optional:** refresh the MCP registry listing with `server.json`
   (now 0.2.0), the same way it was listed at 0.1.1.
4. **Claude**, once both are live: install each from its registry in a
   clean place, merge `docs-proxy`, deploy, check the page.

## Not claimed anywhere

No adoption, no customers, no uptime record. Lab numbers are labelled as
our own agents. The wrapped pair's result goes in the report whichever way
it comes out.
